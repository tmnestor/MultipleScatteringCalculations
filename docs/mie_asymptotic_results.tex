% Auto-generated by Mathematica/MieAsymptotic.wl --- do not edit by hand.
\section{Asymptotic Mie coefficients ($ka \to 0$, exact contrasts)}
\label{sec:mie-asymptotic}

All expressions below are non-dimensionalized with $a = \mu_0 = \rho_0 = 1$.
Contrasts: $\Delta\lambda = \mu_0\,d\lambda$, $\Delta\mu = \mu_0\,d\mu$, $\Delta\rho = \rho_0\,d\rho$.

\begin{equation}
a_0^{\,(\text{full})} \;=\; \frac{w^2 (-3 \text{dlam}-2 \text{dmu})}{3 (\text{lam0}+2) (3 \text{dlam}+2 \text{dmu}+3 \text{lam0}+6)}
\label{eq:mie-a0-full}
\end{equation}

\begin{equation}
a_0^{\,(\text{Born})} \;=\; \frac{w^2 (-3 \text{dlam}-2 \text{dmu})}{9 (\text{lam0}+2)^2}
\label{eq:mie-a0-born}
\end{equation}

\begin{equation}
E_0 \;=\; \frac{3 (\text{lam0}+2)}{3 \text{dlam}+2 \text{dmu}+3 \text{lam0}+6}
\label{eq:mie-E0}
\end{equation}

\begin{equation}
a_1^{\,(\text{full})} \;=\; \frac{i w^4 \left(30 \text{dlam} \text{dmu} \text{drho}^2 \text{lam0}+75 \text{dlam} \text{dmu} \text{drho}^2-45 \text{dlam} \text{dmu} \text{drho}-27 \text{dlam} \text{dmu}+60 \text{dlam} \text{drho}^2 \text{lam0}+145 \text{dlam} \text{drho}^2-75 \text{dlam} \text{drho}-45 \text{dlam}+20 \text{dmu}^2 \text{drho}^2 \text{lam0}+50 \text{dmu}^2 \text{drho}^2-30 \text{dmu}^2 \text{drho}-18 \text{dmu}^2+30 \text{dmu} \text{drho}^2 \text{lam0}^2+202 \text{dmu} \text{drho}^2 \text{lam0}+314 \text{dmu} \text{drho}^2-51 \text{dmu} \text{drho} \text{lam0}-192 \text{dmu} \text{drho}-54 \text{dmu}+60 \text{drho}^2 \text{lam0}^2+270 \text{drho}^2 \text{lam0}+300 \text{drho}^2-45 \text{drho} \text{lam0}-90 \text{drho}\right)}{135 (\text{lam0}+2)^2 \left(3 \text{dlam} \text{dmu}+5 \text{dlam}+2 \text{dmu}^2+3 \text{dmu} \text{lam0}+12 \text{dmu}+5 \text{lam0}+10\right)}+\frac{i \text{drho} w^2}{3 (\text{lam0}+2)}
\label{eq:mie-a1-full}
\end{equation}

\begin{equation}
a_1^{\,(\text{Born})} \;=\; \frac{i \text{drho} w^2}{3 (\text{lam0}+2)}-\frac{i w^4 (5 \text{dlam}+6 \text{dmu}+5 \text{drho} \text{lam0}+10 \text{drho})}{75 (\text{lam0}+2)^3}
\label{eq:mie-a1-born}
\end{equation}

\begin{equation}
E_1 \;=\; 1
\label{eq:mie-E1}
\end{equation}

\begin{equation}
a_2^{\,(\text{full})} \;=\; \left(-\frac{525 (\text{dmu}+1)^2 \left(-\frac{2 \text{dmu} (\text{drho}+1)^5}{6237 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{\text{dmu} (\text{drho}+1)^5}{4455 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{\left(14 \text{lam0} \text{dmu}^2+14 \text{dmu}^2-60 \text{drho} \text{dmu}-30 \text{drho} \text{lam0} \text{dmu}+19 \text{lam0} \text{dmu}+24 \text{dmu}+35 \text{lam0}+70\right) (\text{drho}+1)^4}{13230 (\text{dmu}+1) (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{17 \left(-28 \text{lam0} \text{dmu}^2-28 \text{dmu}^2-14 \text{lam0}^2 \text{dmu}-14 \text{dlam} \text{dmu}+120 \text{drho} \text{dmu}-14 \text{dlam} \text{lam0} \text{dmu}+60 \text{drho} \text{lam0} \text{dmu}-52 \text{lam0} \text{dmu}-48 \text{dmu}+35 \text{drho} \text{lam0}^2+70 \text{dlam} \text{drho}+140 \text{drho}+35 \text{dlam} \text{drho} \text{lam0}+140 \text{drho} \text{lam0}\right) (\text{drho}+1)^4}{198450 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{2 \text{dmu} (2 \text{dlam}+7 \text{dmu}+2 \text{lam0}+7) (\text{drho}+1)^3}{4725 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{\left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right) (\text{drho}+1)^3}{13230 (\text{dmu}+1)^2 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{\left(84 \text{dmu}^4+120 \text{dlam} \text{dmu}^3-110 \text{drho} \text{dmu}^3+120 \text{lam0} \text{dmu}^3+394 \text{dmu}^3+39 \text{dlam}^2 \text{dmu}^2+39 \text{lam0}^2 \text{dmu}^2+331 \text{dlam} \text{dmu}^2-161 \text{dlam} \text{drho} \text{dmu}^2-562 \text{drho} \text{dmu}^2+78 \text{dlam} \text{lam0} \text{dmu}^2-161 \text{drho} \text{lam0} \text{dmu}^2+331 \text{lam0} \text{dmu}^2+446 \text{dmu}^2+55 \text{dlam}^2 \text{dmu}-47 \text{drho} \text{lam0}^2 \text{dmu}+55 \text{lam0}^2 \text{dmu}+166 \text{dlam} \text{dmu}-47 \text{dlam}^2 \text{drho} \text{dmu}-458 \text{dlam} \text{drho} \text{dmu}-794 \text{drho} \text{dmu}+110 \text{dlam} \text{lam0} \text{dmu}-94 \text{dlam} \text{drho} \text{lam0} \text{dmu}-458 \text{drho} \text{lam0} \text{dmu}+166 \text{lam0} \text{dmu}+46 \text{dmu}-63 \text{drho} \text{lam0}^2-45 \text{dlam}-63 \text{dlam}^2 \text{drho}-297 \text{dlam} \text{drho}-342 \text{drho}-126 \text{dlam} \text{drho} \text{lam0}-297 \text{drho} \text{lam0}-45 \text{lam0}-90\right) (\text{drho}+1)^3}{11025 (\text{dmu}+1)^3 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{\left(504 \text{dmu}^4+558 \text{dlam} \text{dmu}^3+1820 \text{drho} \text{dmu}^3+910 \text{drho} \text{lam0} \text{dmu}^3+586 \text{lam0} \text{dmu}^3+2324 \text{dmu}^3+153 \text{dlam}^2 \text{dmu}^2+742 \text{drho} \text{lam0}^2 \text{dmu}^2+202 \text{lam0}^2 \text{dmu}^2+1970 \text{dlam} \text{dmu}^2+1484 \text{dlam} \text{drho} \text{dmu}^2+4648 \text{drho} \text{dmu}^2+355 \text{dlam} \text{lam0} \text{dmu}^2+742 \text{dlam} \text{drho} \text{lam0} \text{dmu}^2+3808 \text{drho} \text{lam0} \text{dmu}^2+1648 \text{lam0} \text{dmu}^2+3136 \text{dmu}^2+154 \text{drho} \text{lam0}^3 \text{dmu}+28 \text{lam0}^3 \text{dmu}+398 \text{dlam}^2 \text{dmu}+56 \text{dlam} \text{lam0}^2 \text{dmu}+308 \text{dlam} \text{drho} \text{lam0}^2 \text{dmu}+1554 \text{drho} \text{lam0}^2 \text{dmu}+258 \text{lam0}^2 \text{dmu}+1790 \text{dlam} \text{dmu}+308 \text{dlam}^2 \text{drho} \text{dmu}+2492 \text{dlam} \text{drho} \text{dmu}+3836 \text{drho} \text{dmu}+28 \text{dlam}^2 \text{lam0} \text{dmu}+656 \text{dlam} \text{lam0} \text{dmu}+154 \text{dlam}^2 \text{drho} \text{lam0} \text{dmu}+1862 \text{dlam} \text{drho} \text{lam0} \text{dmu}+4410 \text{drho} \text{lam0} \text{dmu}+1062 \text{lam0} \text{dmu}+1316 \text{dmu}+126 \text{drho} \text{lam0}^3+189 \text{dlam}^2+252 \text{dlam} \text{drho} \text{lam0}^2+756 \text{drho} \text{lam0}^2+378 \text{dlam}+252 \text{dlam}^2 \text{drho}+1008 \text{dlam} \text{drho}+1008 \text{drho}+189 \text{dlam} \text{lam0}+126 \text{dlam}^2 \text{drho} \text{lam0}+1008 \text{dlam} \text{drho} \text{lam0}+1512 \text{drho} \text{lam0}\right) (\text{drho}+1)^3}{99225 (\text{dmu}+1)^3 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{dmu}+1) \left(-\frac{5 (\text{drho}+1)^3}{63 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}+\frac{6 (\text{lam0}+1) (\text{drho}+1)^2}{35 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}+\frac{(3 \text{lam0}+5) (\text{drho}+1)}{21 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}-\frac{4 \left(9 \text{lam0} \text{dmu}^2+12 \text{dmu}^2+6 \text{drho} \text{lam0}^2 \text{dmu}+6 \text{lam0}^2 \text{dmu}-12 \text{drho} \text{dmu}+6 \text{drho} \text{lam0} \text{dmu}+24 \text{lam0} \text{dmu}+12 \text{dmu}-16 \text{drho}^2-4 \text{drho}^2 \text{lam0}^2-2 \text{drho} \text{lam0}^2+2 \text{lam0}^2-44 \text{drho}-16 \text{drho}^2 \text{lam0}-26 \text{drho} \text{lam0}-\text{lam0}-16\right) (\text{drho}+1)}{315 (\text{dmu}+1)^3 (\text{lam0}+2)^{5/2}}-\frac{4}{5} \left(\frac{1}{12 \sqrt{\text{lam0}+2}}-\frac{1}{14 (\text{lam0}+2)^{3/2}}+\frac{1}{28 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)+\frac{(\text{dmu}-\text{drho}) (-7 \text{lam0}-12) (\text{drho}+1)}{35 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}-\frac{4 i \left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right)}{3 \sqrt{\text{lam0}+2}}\right) (\text{drho}+1)^2}{35 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{8 \left(-\frac{(3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (\text{drho}+1)^3}{63 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{(\text{lam0}+1) (7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) (\text{drho}+1)^2}{105 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(3 \text{lam0}+5) (\text{drho}+1)}{63 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{4 (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)}{105 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{4 (\text{dmu}+1) \left(\frac{1}{12 \sqrt{\text{lam0}+2}}-\frac{1}{14 (\text{lam0}+2)^{3/2}}+\frac{1}{28 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{(-7 \text{lam0}-12) (-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (\text{drho}+1)}{105 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{4 i \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{3 \sqrt{\text{lam0}+2}}-144 i \left(-\frac{i (\text{drho}+1)^3}{5670 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)}{11340 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{3780 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right) (\text{drho}+1)^2}{105 (\text{dmu}+1)}+\frac{2 (\text{dmu}+1) \left(\frac{(\text{drho}+1)^4}{594 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}-\frac{(\text{lam0}+1) (\text{drho}+1)^3}{126 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{6 \left(\frac{1}{12 \sqrt{\text{lam0}+2}}-\frac{1}{14 (\text{lam0}+2)^{3/2}}+\frac{1}{28 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}+\frac{(\text{dmu}-\text{drho}) (3 \text{lam0}+5) (\text{drho}+1)}{210 (\text{dmu}+1)^2 (\text{lam0}+2)^{5/2}}-\frac{(33 \text{lam0}+56) (\text{drho}+1)}{8316 (\text{dmu}+1) (\text{lam0}+2)^{7/2}}-\frac{4}{5} \left(\frac{1}{72 \sqrt{\text{lam0}+2}}+\frac{1}{168 (\text{lam0}+2)^{3/2}}+\frac{1}{504 (\text{lam0}+2)^{5/2}}-\frac{1}{1512 (\text{lam0}+2)^{7/2}}\right) (\text{drho}+1)-12 i \left(\frac{i (\text{drho}+1)^3}{2835 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}+\frac{i (\text{drho}+1)^2}{630 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)}{1890 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}\right)-144 i \left(-\frac{i (\text{drho}+1)^4}{124740 (\text{dmu}+1)^4 \sqrt{\text{lam0}+2}}-\frac{11 i (\text{drho}+1)^3}{158760 (\text{dmu}+1)^3 (\text{lam0}+2)^{3/2}}-\frac{i (\text{drho}+1)^2}{13230 (\text{dmu}+1)^2 (\text{lam0}+2)^{5/2}}-\frac{i (\text{drho}+1)}{83160 (\text{dmu}+1) (\text{lam0}+2)^{7/2}}\right)-\frac{4 i \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right)}{3 \sqrt{\text{lam0}+2}}-\frac{i \left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) (-7 \text{lam0}-12)}{21 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{2}{5} \left(\frac{(33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^4}{24948 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(\text{lam0}+1) (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (\text{drho}+1)^3}{630 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(\frac{1}{12 \sqrt{\text{lam0}+2}}-\frac{1}{14 (\text{lam0}+2)^{3/2}}+\frac{1}{28 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (3 \text{lam0}+5) (\text{drho}+1)}{630 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(33 \text{lam0}+56) (\text{drho}+1)}{24948 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{4 (\text{dmu}+1) \left(\frac{1}{72 \sqrt{\text{lam0}+2}}+\frac{1}{168 (\text{lam0}+2)^{3/2}}+\frac{1}{504 (\text{lam0}+2)^{5/2}}-\frac{1}{1512 (\text{lam0}+2)^{7/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{4 i \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{3 \sqrt{\text{lam0}+2}}-\frac{i (-7 \text{lam0}-12) \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{21 (\text{lam0}+2)^{3/2}}-144 i \left(\frac{i (\text{drho}+1)^4}{249480 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{158760 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{158760 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{748440 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{187110 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-12 i \left(-\frac{i (\text{drho}+1)^3}{5670 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)}{11340 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{3780 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right) (\text{drho}+1)+\frac{2 i \left(-\frac{i (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^5}{41580 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^5}{2970 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (\text{dmu}-\text{drho}) (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (\text{drho}+1)^4}{1050 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{2 i \left(2 \text{dlam}^2+17 \text{dmu} \text{dlam}+4 \text{lam0} \text{dlam}+17 \text{dlam}+29 \text{dmu}^2+2 \text{lam0}^2+58 \text{dmu}+17 \text{dmu} \text{lam0}+17 \text{lam0}+29\right) (\text{drho}+1)^4}{4725 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (\text{drho}+1)^4}{630 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{\left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) (7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{6 \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}-\frac{4}{5} \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)+\frac{4 (\text{dmu}+1) \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+144 i \left(-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{11 (\text{drho}+1)^5}{793800 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{drho}+1)^5}{66150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(-\text{drho}-1) (\text{drho}+1)^4}{1247400 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)}{3 \sqrt{\text{lam0}+2}}-48 i \left(-\frac{i (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)^4}{39690 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (3 \text{dmu}+4 \text{drho}+2 \text{drho} \text{lam0}+2 \text{lam0}+7) (\text{drho}+1)^4}{198450 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (-7 \text{lam0}-12) \left(2 \text{dlam}^2+17 \text{dmu} \text{dlam}+4 \text{lam0} \text{dlam}+17 \text{dlam}+29 \text{dmu}^2+2 \text{lam0}^2+58 \text{dmu}+17 \text{dmu} \text{lam0}+17 \text{lam0}+29\right) (\text{drho}+1)^4}{595350 (\text{dmu}+1)^3 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (2 \text{dlam}+7 \text{dmu}+2 \text{lam0}+7) (3 \text{lam0}+5) (\text{drho}+1)^3}{198450 (\text{dmu}+1)^2 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(\frac{i (\text{drho}+1)^3}{2835 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}+\frac{i (\text{drho}+1)^2}{630 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)}{1890 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{6 \left(-\frac{i (\text{drho}+1)^3}{5670 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)}{11340 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{3780 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}+\frac{4 (\text{dmu}+1) \left(-\frac{i (\text{drho}+1)^4}{124740 (\text{dmu}+1)^4 \sqrt{\text{lam0}+2}}-\frac{11 i (\text{drho}+1)^3}{158760 (\text{dmu}+1)^3 (\text{lam0}+2)^{3/2}}-\frac{i (\text{drho}+1)^2}{13230 (\text{dmu}+1)^2 (\text{lam0}+2)^{5/2}}-\frac{i (\text{drho}+1)}{83160 (\text{dmu}+1) (\text{lam0}+2)^{7/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{4}{5} \left(\frac{i (\text{drho}+1)^4}{249480 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{158760 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{158760 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{748440 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{187110 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)-\frac{4 i \left(-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{11 (\text{drho}+1)^5}{793800 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{drho}+1)^5}{66150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(-\text{drho}-1) (\text{drho}+1)^4}{1247400 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{3 \sqrt{\text{lam0}+2}}\right)\right) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}{(\text{drho}+1)^3 \sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)}+\frac{20 \text{dmu} \left(\frac{525 (\text{dmu}+1)^2 \left(-\frac{\sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) (\text{drho}+1)^5}{20790 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{\sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) (\text{drho}+1)^5}{29700 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{\left(42 \text{lam0}^2 \text{dmu}^2+182 \text{lam0} \text{dmu}^2+224 \text{dmu}^2-18 \text{drho} \text{lam0}^2 \text{dmu}+45 \text{lam0}^2 \text{dmu}-96 \text{drho} \text{dmu}-84 \text{drho} \text{lam0} \text{dmu}+217 \text{lam0} \text{dmu}+282 \text{dmu}-21 \text{drho} \text{lam0}^2-84 \text{drho}-84 \text{drho} \text{lam0}+35 \text{lam0}+70\right) (\text{drho}+1)^4}{8820 (\text{dmu}+1) \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{17 \left(-42 \text{dmu} \text{lam0}^3+21 \text{drho} \text{lam0}^3-84 \text{dmu}^2 \text{lam0}^2-42 \text{dlam} \text{dmu} \text{lam0}^2-272 \text{dmu} \text{lam0}^2+21 \text{dlam} \text{drho} \text{lam0}^2+36 \text{dmu} \text{drho} \text{lam0}^2+161 \text{drho} \text{lam0}^2-364 \text{dmu}^2 \text{lam0}-21 \text{dlam} \text{lam0}-182 \text{dlam} \text{dmu} \text{lam0}-658 \text{dmu} \text{lam0}+98 \text{dlam} \text{drho} \text{lam0}+168 \text{dmu} \text{drho} \text{lam0}+392 \text{drho} \text{lam0}-448 \text{dmu}^2-42 \text{dlam}-224 \text{dlam} \text{dmu}-564 \text{dmu}+112 \text{dlam} \text{drho}+192 \text{dmu} \text{drho}+308 \text{drho}\right) (\text{drho}+1)^4}{132300 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{lam0}+2)^{3/2} \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right) (\text{drho}+1)^3}{840 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{\left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right) (\text{drho}+1)^3}{1050 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{\sqrt{\text{lam0}+2} \left(-28 \text{dmu}^4-76 \text{dlam} \text{dmu}^3+220 \text{drho} \text{dmu}^3+110 \text{drho} \text{lam0} \text{dmu}^3+118 \text{lam0} \text{dmu}^3+52 \text{dmu}^3-31 \text{dlam}^2 \text{dmu}^2+161 \text{drho} \text{lam0}^2 \text{dmu}^2+196 \text{lam0}^2 \text{dmu}^2+50 \text{dlam} \text{dmu}^2+322 \text{dlam} \text{drho} \text{dmu}^2+1124 \text{drho} \text{dmu}^2+165 \text{dlam} \text{lam0} \text{dmu}^2+161 \text{dlam} \text{drho} \text{lam0} \text{dmu}^2+884 \text{drho} \text{lam0} \text{dmu}^2+864 \text{lam0} \text{dmu}^2+788 \text{dmu}^2+47 \text{drho} \text{lam0}^3 \text{dmu}+59 \text{lam0}^3 \text{dmu}+24 \text{dlam}^2 \text{dmu}+118 \text{dlam} \text{lam0}^2 \text{dmu}+94 \text{dlam} \text{drho} \text{lam0}^2 \text{dmu}+552 \text{drho} \text{lam0}^2 \text{dmu}+614 \text{lam0}^2 \text{dmu}+600 \text{dlam} \text{dmu}+94 \text{dlam}^2 \text{drho} \text{dmu}+916 \text{dlam} \text{drho} \text{dmu}+1588 \text{drho} \text{dmu}+59 \text{dlam}^2 \text{lam0} \text{dmu}+638 \text{dlam} \text{lam0} \text{dmu}+47 \text{dlam}^2 \text{drho} \text{lam0} \text{dmu}+646 \text{dlam} \text{drho} \text{lam0} \text{dmu}+1710 \text{drho} \text{lam0} \text{dmu}+1646 \text{lam0} \text{dmu}+1308 \text{dmu}+63 \text{drho} \text{lam0}^3+75 \text{lam0}^3+87 \text{dlam}^2+150 \text{dlam} \text{lam0}^2+126 \text{dlam} \text{drho} \text{lam0}^2+423 \text{drho} \text{lam0}^2+450 \text{lam0}^2+474 \text{dlam}+126 \text{dlam}^2 \text{drho}+594 \text{dlam} \text{drho}+684 \text{drho}+75 \text{dlam}^2 \text{lam0}+537 \text{dlam} \text{lam0}+63 \text{dlam}^2 \text{drho} \text{lam0}+549 \text{dlam} \text{drho} \text{lam0}+936 \text{drho} \text{lam0}+900 \text{lam0}+600\right) (\text{drho}+1)^3}{6300 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{dmu}+1) \left(-\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (\text{drho}+1)^3}{42 (\text{dmu}+1)^2}+\frac{9 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^2}{35 (\text{dmu}+1) \sqrt{\text{lam0}+2}}-\frac{3 (\text{lam0}+2)^{3/2} (\text{drho}+1)}{2 (\text{dmu}+1)}-\frac{4}{5} \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)-\frac{3 (\text{dmu}-\text{drho}) \text{lam0} (\text{drho}+1)}{10 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{3 (\text{drho}+1)}{4 (\text{dmu}+1) \sqrt{\text{lam0}+2}}-72 i \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) (\text{lam0}+2)^{3/2}+i \left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) \sqrt{\text{lam0}+2} (3 \text{lam0}-2)-12 i \left(-\frac{3 i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dmu}+1)^3}+\frac{11 i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{420 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{40 (\text{dmu}+1) \sqrt{\text{lam0}+2}}\right)-144 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right)\right) (\text{drho}+1)^2}{35 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{8 \left(-\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (\text{drho}+1)^3}{210 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^2}{70 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{4 (\text{dmu}+1) \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{(\text{lam0}+2)^{3/2} (\text{drho}+1)}{2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{\text{drho}+1}{4 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) \text{lam0} (\text{drho}+1)}{10 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-72 i (\text{lam0}+2)^{3/2} \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-144 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-12 i \left(-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{84 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{60 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{120 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right) (\text{drho}+1)^2}{105 (\text{dmu}+1)}+\frac{2 (\text{dmu}+1) \left(\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (\text{drho}+1)^4}{1980 (\text{dmu}+1)^3}-\frac{\left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^3}{84 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}+\frac{6 \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}+\frac{(\text{lam0}+2)^{3/2} (\text{drho}+1)}{8 (\text{dmu}+1)}+\frac{(3 \text{lam0}+5) (\text{drho}+1)}{24 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}+\frac{3 \sqrt{\text{lam0}+2} (-\text{dmu}+2 \text{drho}+\text{drho} \text{lam0}+\text{lam0}+1) (\text{drho}+1)}{20 (\text{dmu}+1)^2}-\frac{4}{5} \left(\frac{1}{16} (\text{lam0}+2)^{3/2}-\frac{5 \sqrt{\text{lam0}+2}}{48}+\frac{1}{16 \sqrt{\text{lam0}+2}}-\frac{1}{16 (\text{lam0}+2)^{3/2}}+\frac{1}{24 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)-\frac{3 (\text{dmu}-\text{drho}) (\text{drho}+1)}{40 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-72 i \left(-\frac{i (\text{drho}+1)^4}{18480 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{2016 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{960 (\text{dmu}+1)}\right) (\text{lam0}+2)^{3/2}+i \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) \sqrt{\text{lam0}+2} (3 \text{lam0}-2)-144 i \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{66528 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{60480 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}+\frac{i (\text{drho}+1)^2}{1120 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)}{1920 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}\right)-12 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right)+\frac{i \left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) \text{lam0}}{2 \sqrt{\text{lam0}+2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{2}{5} \left(\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^4}{83160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) \left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^3}{420 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(3 \text{lam0}+5) (\text{drho}+1)}{72 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{\sqrt{\text{lam0}+2} (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{4 (\text{dmu}+1) \left(\frac{1}{16} (\text{lam0}+2)^{3/2}-\frac{5 \sqrt{\text{lam0}+2}}{48}+\frac{1}{16 \sqrt{\text{lam0}+2}}-\frac{1}{16 (\text{lam0}+2)^{3/2}}+\frac{1}{24 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(\text{lam0}+2)^{3/2} (\text{drho}+1)}{24 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (\text{drho}+1)}{40 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-72 i (\text{lam0}+2)^{3/2} \left(-\frac{i (\text{drho}+1)^4}{166320 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{10080 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{1008 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{1440 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{2880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+\frac{i \text{lam0} \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{2 \sqrt{\text{lam0}+2}}-144 i \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{110880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{12096 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{10080 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{8640 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{3456 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-12 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right) (\text{drho}+1)-24 i (\text{lam0}+2)^{3/2} \left(-\frac{i (\text{dmu}-\text{drho}) (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^5}{415800 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (\text{drho}+1)^5}{29700 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (2 \text{dlam}+7 \text{dmu}+2 \text{lam0}+7) (\text{drho}+1)^3}{3150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{\left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (\text{drho}+1)^3}{630 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{\left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^3}{126 (\text{dmu}+1)^2}-\frac{\left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) (7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{6 \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}-\frac{4}{5} \left(-\frac{i (\text{drho}+1)^4}{166320 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{10080 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{1008 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{1440 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{2880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)+\frac{4 (\text{dmu}+1) \left(-\frac{i (\text{drho}+1)^4}{18480 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{2016 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{960 (\text{dmu}+1)}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+144 i \left(\frac{(\text{drho}+1)^6}{3492720 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(\text{drho}+1)^6}{1587600 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{17 (\text{drho}+1)^6}{52390800 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}\right)+12 i \left(-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{11 (\text{drho}+1)^5}{793800 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{drho}+1)^5}{66150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(-\text{drho}-1) (\text{drho}+1)^4}{1247400 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)-2 i \sqrt{\text{lam0}+2} \left(-\frac{i (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^5}{41580 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^5}{2970 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (\text{dmu}-\text{drho}) (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (\text{drho}+1)^4}{1050 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{2 i \left(2 \text{dlam}^2+17 \text{dmu} \text{dlam}+4 \text{lam0} \text{dlam}+17 \text{dlam}+29 \text{dmu}^2+2 \text{lam0}^2+58 \text{dmu}+17 \text{dmu} \text{lam0}+17 \text{lam0}+29\right) (\text{drho}+1)^4}{4725 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (\text{drho}+1)^4}{630 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{\left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) (7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{6 \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}-\frac{4}{5} \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)+\frac{4 (\text{dmu}+1) \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+144 i \left(-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{11 (\text{drho}+1)^5}{793800 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{drho}+1)^5}{66150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(-\text{drho}-1) (\text{drho}+1)^4}{1247400 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)-3 i \left(\frac{i (\text{lam0}+2)^{3/2} (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^5}{83160 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^5}{5940 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i \sqrt{\text{lam0}+2} (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)^4}{1260 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i \sqrt{\text{lam0}+2} (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (-\text{dmu}+2 \text{drho}+\text{drho} \text{lam0}+\text{lam0}+1) (\text{drho}+1)^4}{2100 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(2 \text{dlam}^2+17 \text{dmu} \text{dlam}+4 \text{lam0} \text{dlam}+17 \text{dlam}+29 \text{dmu}^2+2 \text{lam0}^2+58 \text{dmu}+17 \text{dmu} \text{lam0}+17 \text{lam0}+29\right) (\text{drho}+1)^4}{28350 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i \text{lam0} (2 \text{dlam}+7 \text{dmu}+2 \text{lam0}+7) (\text{drho}+1)^3}{3150 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(-\frac{3 i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dmu}+1)^3}+\frac{11 i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{420 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{40 (\text{dmu}+1) \sqrt{\text{lam0}+2}}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{6 \left(-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{84 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{60 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{120 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}+\frac{4 (\text{dmu}+1) \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{4}{5} \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)-72 i (\text{lam0}+2)^{3/2} \left(-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{11 (\text{drho}+1)^5}{793800 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{drho}+1)^5}{66150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(-\text{drho}-1) (\text{drho}+1)^4}{1247400 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)-48 i \left(\frac{i \sqrt{\text{lam0}+2} (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)^5}{59400 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i \sqrt{\text{lam0}+2} (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (-\text{dmu}+2 \text{drho}+\text{drho} \text{lam0}+\text{lam0}+1) (\text{drho}+1)^5}{831600 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i \text{lam0} \left(2 \text{dlam}^2+17 \text{dmu} \text{dlam}+4 \text{lam0} \text{dlam}+17 \text{dlam}+29 \text{dmu}^2+2 \text{lam0}^2+58 \text{dmu}+17 \text{dmu} \text{lam0}+17 \text{lam0}+29\right) (\text{drho}+1)^4}{56700 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (2 \text{dlam}+7 \text{dmu}+2 \text{lam0}+7) (\text{drho}+1)^3}{12600 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) \left(-\frac{3 i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dmu}+1)^3}+\frac{11 i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{420 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{40 (\text{dmu}+1) \sqrt{\text{lam0}+2}}\right) (\text{drho}+1)^3}{630 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{\left(-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{84 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{60 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{120 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^3}{126 (\text{dmu}+1)^2}-\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{6 \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}+\frac{4 (\text{dmu}+1) \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{66528 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{60480 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}+\frac{i (\text{drho}+1)^2}{1120 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)}{1920 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{4}{5} \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{110880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{12096 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{10080 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{8640 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{3456 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)-72 i (\text{lam0}+2)^{3/2} \left(\frac{(\text{drho}+1)^6}{3492720 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(\text{drho}+1)^6}{1587600 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{17 (\text{drho}+1)^6}{52390800 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}\right)+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{11 (\text{drho}+1)^5}{793800 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{drho}+1)^5}{66150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(-\text{drho}-1) (\text{drho}+1)^4}{1247400 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)\right) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}{(\text{drho}+1)^3 \sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)}+\frac{\left(504 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^5+4 \left(702 \text{lam0}^3+6036 \text{lam0}^2+16771 \text{lam0}-73 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+16094\right) \text{dmu}^4+2 \left(513 \text{lam0}^4+8976 \text{lam0}^3+45815 \text{lam0}^2+96812 \text{lam0}-\text{drho} \left(699 \text{lam0}^3+6745 \text{lam0}^2+19504 \text{lam0}+17620\right)+75620\right) \text{dmu}^3-(\text{lam0}+2) \left(-3147 \text{lam0}^3-26458 \text{lam0}^2-72604 \text{lam0}+\text{drho} \left(444 \text{lam0}^3+8399 \text{lam0}^2+35576 \text{lam0}+42092\right)-70804\right) \text{dmu}^2-3 (\text{lam0}+2)^2 \left(\text{drho} \left(505 \text{lam0}^2+4024 \text{lam0}+6778\right)-5 \left(151 \text{lam0}^2+604 \text{lam0}+778\right)\right) \text{dmu}-45 \text{drho} (\text{lam0}+2)^3 (27 \text{lam0}+77)+\text{dlam}^2 \left(342 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^3+\left(3147 \text{lam0}^2+13885 \text{lam0}-148 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+17126\right) \text{dmu}^2-3 \left(-755 \text{lam0}^2-3632 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+2224 \text{lam0}+2428\right)-4664\right) \text{dmu}-9 (\text{lam0}+2) (\text{drho} (135 \text{lam0}+298)-105)\right)+\text{dlam} \left(936 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^4+\left(2052 \text{lam0}^3+22398 \text{lam0}^2+70048 \text{lam0}-466 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+72968\right) \text{dmu}^3+\left(6294 \text{lam0}^3+46637 \text{lam0}^2+120308 \text{lam0}-\text{drho} \left(888 \text{lam0}^3+11359 \text{lam0}^2+37012 \text{lam0}+35692\right)+108596\right) \text{dmu}^2-6 (\text{lam0}+2) \left(-755 \text{lam0}^2-3326 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+3124 \text{lam0}+4402\right)-4532\right) \text{dmu}-9 (\text{lam0}+2)^2 (\text{drho} (270 \text{lam0}+683)-105)\right)\right)^2}{324 (\text{dmu}+1)^2 (\text{lam0}+2)^2 (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30)^2 \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}\right)}{3 (\text{lam0}+2) (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30)}-\frac{\left(252 \text{lam0} \text{dmu}^5+252 \text{dmu}^5+468 \text{lam0}^2 \text{dmu}^4+468 \text{dlam} \text{dmu}^4-1460 \text{drho} \text{dmu}^4+468 \text{dlam} \text{lam0} \text{dmu}^4-730 \text{drho} \text{lam0} \text{dmu}^4+1754 \text{lam0} \text{dmu}^4+556 \text{dmu}^4+171 \text{lam0}^3 \text{dmu}^3+171 \text{dlam}^2 \text{dmu}^3+342 \text{dlam} \text{lam0}^2 \text{dmu}^3-1165 \text{drho} \text{lam0}^2 \text{dmu}^3+1472 \text{lam0}^2 \text{dmu}^3+406 \text{dlam} \text{dmu}^3-2330 \text{dlam} \text{drho} \text{dmu}^3-9220 \text{drho} \text{dmu}^3+171 \text{dlam}^2 \text{lam0} \text{dmu}^3+1643 \text{dlam} \text{lam0} \text{dmu}^3-1165 \text{dlam} \text{drho} \text{lam0} \text{dmu}^3-6940 \text{drho} \text{lam0} \text{dmu}^3+1592 \text{lam0} \text{dmu}^3-2164 \text{dmu}^3-370 \text{drho} \text{lam0}^3 \text{dmu}^2+251 \text{lam0}^3 \text{dmu}^2+16 \text{dlam}^2 \text{dmu}^2+502 \text{dlam} \text{lam0}^2 \text{dmu}^2-740 \text{dlam} \text{drho} \text{lam0}^2 \text{dmu}^2-5010 \text{drho} \text{lam0}^2 \text{dmu}^2+534 \text{lam0}^2 \text{dmu}^2-1952 \text{dlam} \text{dmu}^2-740 \text{dlam}^2 \text{drho} \text{dmu}^2-8540 \text{dlam} \text{drho} \text{dmu}^2-16580 \text{drho} \text{dmu}^2+251 \text{dlam}^2 \text{lam0} \text{dmu}^2+550 \text{dlam} \text{lam0} \text{dmu}^2-370 \text{dlam}^2 \text{drho} \text{lam0} \text{dmu}^2-5750 \text{dlam} \text{drho} \text{lam0} \text{dmu}^2-16830 \text{drho} \text{lam0} \text{dmu}^2-2430 \text{lam0} \text{dmu}^2-4988 \text{dmu}^2-765 \text{drho} \text{lam0}^3 \text{dmu}-315 \text{dlam}^2 \text{dmu}-1530 \text{dlam} \text{drho} \text{lam0}^2 \text{dmu}-5895 \text{drho} \text{lam0}^2 \text{dmu}-630 \text{lam0}^2 \text{dmu}-1890 \text{dlam} \text{dmu}-1530 \text{dlam}^2 \text{drho} \text{dmu}-8730 \text{dlam} \text{drho} \text{dmu}-11340 \text{drho} \text{dmu}-945 \text{dlam} \text{lam0} \text{dmu}-765 \text{dlam}^2 \text{drho} \text{lam0} \text{dmu}-7425 \text{dlam} \text{drho} \text{lam0} \text{dmu}-14400 \text{drho} \text{lam0} \text{dmu}-2520 \text{lam0} \text{dmu}-2520 \text{dmu}-315 \text{drho} \text{lam0}^3-630 \text{dlam} \text{drho} \text{lam0}^2-1890 \text{drho} \text{lam0}^2-630 \text{dlam}^2 \text{drho}-2520 \text{dlam} \text{drho}-2520 \text{drho}-315 \text{dlam}^2 \text{drho} \text{lam0}-2520 \text{dlam} \text{drho} \text{lam0}-3780 \text{drho} \text{lam0}\right) \left(504 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^5+4 \left(702 \text{lam0}^3+6036 \text{lam0}^2+16771 \text{lam0}-73 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+16094\right) \text{dmu}^4+2 \left(513 \text{lam0}^4+8976 \text{lam0}^3+45815 \text{lam0}^2+96812 \text{lam0}-\text{drho} \left(699 \text{lam0}^3+6745 \text{lam0}^2+19504 \text{lam0}+17620\right)+75620\right) \text{dmu}^3-(\text{lam0}+2) \left(-3147 \text{lam0}^3-26458 \text{lam0}^2-72604 \text{lam0}+\text{drho} \left(444 \text{lam0}^3+8399 \text{lam0}^2+35576 \text{lam0}+42092\right)-70804\right) \text{dmu}^2-3 (\text{lam0}+2)^2 \left(\text{drho} \left(505 \text{lam0}^2+4024 \text{lam0}+6778\right)-5 \left(151 \text{lam0}^2+604 \text{lam0}+778\right)\right) \text{dmu}-45 \text{drho} (\text{lam0}+2)^3 (27 \text{lam0}+77)+\text{dlam}^2 \left(342 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^3+\left(3147 \text{lam0}^2+13885 \text{lam0}-148 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+17126\right) \text{dmu}^2-3 \left(-755 \text{lam0}^2-3632 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+2224 \text{lam0}+2428\right)-4664\right) \text{dmu}-9 (\text{lam0}+2) (\text{drho} (135 \text{lam0}+298)-105)\right)+\text{dlam} \left(936 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^4+\left(2052 \text{lam0}^3+22398 \text{lam0}^2+70048 \text{lam0}-466 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+72968\right) \text{dmu}^3+\left(6294 \text{lam0}^3+46637 \text{lam0}^2+120308 \text{lam0}-\text{drho} \left(888 \text{lam0}^3+11359 \text{lam0}^2+37012 \text{lam0}+35692\right)+108596\right) \text{dmu}^2-6 (\text{lam0}+2) \left(-755 \text{lam0}^2-3326 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+3124 \text{lam0}+4402\right)-4532\right) \text{dmu}-9 (\text{lam0}+2)^2 (\text{drho} (270 \text{lam0}+683)-105)\right)\right)}{243 (\text{dmu}+1)^2 (\text{lam0}+2)^3 (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30)^2 \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}\right) w^6-\frac{40 i \text{dmu}^2 \left(3 \sqrt{\text{lam0}+2} \text{lam0}^2+12 \sqrt{\text{lam0}+2} \text{lam0}+12 \sqrt{\text{lam0}+2}+2\right) w^5}{9 (\text{lam0}+2)^{5/2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30)^2}-\frac{2 \left(8 \text{lam0}^2 \text{dmu}^2+36 \text{lam0} \text{dmu}^2+48 \text{dmu}^2+10 \text{lam0} \text{dmu}+20 \text{dmu}+5 \text{drho} \text{lam0}^2+20 \text{drho}+20 \text{drho} \text{lam0}\right) w^4}{(\text{lam0}+2)^2 (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30)^2}+\frac{20 \text{dmu} w^2}{3 (\text{lam0}+2) (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30)}
\label{eq:mie-a2-full}
\end{equation}

\begin{equation}
a_2^{\,(\text{Born})} \;=\; \frac{w^6 (7 \text{dlam}+26 \text{dmu}+14 \text{drho} \text{lam0}+28 \text{drho})}{2205 (\text{lam0}+2)^4}-\frac{2 w^4 (2 \text{dmu}+\text{drho} \text{lam0}+2 \text{drho})}{45 (\text{lam0}+2)^3}+\frac{4 \text{dmu} w^2}{9 (\text{lam0}+2)^2}
\label{eq:mie-a2-born}
\end{equation}

\subsection*{Interior displacement to leading order}
\begin{equation}
u_r^{\text{int},(0)}(r,\theta)\,P_0^{-1}(\cos\theta) \;=\; \frac{i \sqrt{\text{lam0}+2} \text{rvar} w}{3 \text{dlam}+2 \text{dmu}+3 \text{lam0}+6}
\label{eq:mie-int-ur-0}
\end{equation}

\begin{equation}
u_r^{\text{int},(1)}(r,\theta)\,P_1^{-1}(\cos\theta) \;=\; \frac{w^2 \left(12 \text{dlam} \text{dmu} \text{drho} \text{lam0}+30 \text{dlam} \text{dmu} \text{drho}-9 \text{dlam} \text{dmu}-6 \text{dlam} \text{drho} \text{lam0} \text{rvar}^2+30 \text{dlam} \text{drho} \text{lam0}-12 \text{dlam} \text{drho} \text{rvar}^2+70 \text{dlam} \text{drho}-15 \text{dlam}+8 \text{dmu}^2 \text{drho} \text{lam0}+20 \text{dmu}^2 \text{drho}-6 \text{dmu}^2+12 \text{dmu} \text{drho} \text{lam0}^2-3 \text{dmu} \text{drho} \text{lam0} \text{rvar}^2+85 \text{dmu} \text{drho} \text{lam0}-6 \text{dmu} \text{drho} \text{rvar}^2+134 \text{dmu} \text{drho}-9 \text{dmu} \text{lam0} \text{rvar}^2-12 \text{dmu} \text{lam0}-18 \text{dmu} \text{rvar}^2-42 \text{dmu}-6 \text{drho} \text{lam0}^2 \text{rvar}^2+30 \text{drho} \text{lam0}^2-33 \text{drho} \text{lam0} \text{rvar}^2+135 \text{drho} \text{lam0}-42 \text{drho} \text{rvar}^2+150 \text{drho}-27 \text{lam0} \text{rvar}^2-54 \text{rvar}^2\right)}{18 (\text{lam0}+2) \left(3 \text{dlam} \text{dmu}+5 \text{dlam}+2 \text{dmu}^2+3 \text{dmu} \text{lam0}+12 \text{dmu}+5 \text{lam0}+10\right)}+\frac{i \text{drho} \left(2 \sqrt{\text{lam0}+2} \text{lam0}+4 \sqrt{\text{lam0}+2}+1\right) w^3}{9 (\text{lam0}+2)^{3/2}}+1
\label{eq:mie-int-ur-1}
\end{equation}

\begin{equation}
u_r^{\text{int},(2)}(r,\theta)\,P_2^{-1}(\cos\theta) \;=\; \left(\sqrt{\frac{\text{drho}+1}{\text{dlam}+2 (\text{dmu}+1)+\text{lam0}}} \left(\frac{5 i \left(14 \text{dmu}^2-2 \text{drho} \text{dmu}+33 \text{dmu}+14 \text{drho}+35\right) \sqrt{\text{lam0}+2} \left(\frac{\text{drho}+1}{\text{dlam}+2 \text{dmu}+\text{lam0}+2}\right)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2 \text{rvar}^5}{84 (\text{drho}+1)^2 (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)}-\frac{2}{105} \left(\frac{\text{drho}+1}{\text{dlam}+2 \text{dmu}+\text{lam0}+2}\right)^{3/2} \left(\frac{25 i \left(126 \text{dmu}^3-54 \text{drho} \text{dmu}^2+261 \text{dmu}^2-2 \text{drho}^2 \text{dmu}-103 \text{drho} \text{dmu}+151 \text{dmu}-18 \text{drho}^2-81 \text{drho}\right) \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}{6 (\text{dmu}+1) (\text{drho}+1)^2 (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)}-\frac{25 i \left(14 \text{dmu}^2-2 \text{drho} \text{dmu}+33 \text{dmu}+14 \text{drho}+35\right) (\text{dlam}+2 \text{dmu}+\text{lam0}+2) \left(504 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^5+4 \left(702 \text{lam0}^3+6036 \text{lam0}^2+16771 \text{lam0}-73 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+16094\right) \text{dmu}^4+2 \left(513 \text{lam0}^4+8976 \text{lam0}^3+45815 \text{lam0}^2+96812 \text{lam0}-\text{drho} \left(699 \text{lam0}^3+6745 \text{lam0}^2+19504 \text{lam0}+17620\right)+75620\right) \text{dmu}^3-(\text{lam0}+2) \left(-3147 \text{lam0}^3-26458 \text{lam0}^2-72604 \text{lam0}+\text{drho} \left(444 \text{lam0}^3+8399 \text{lam0}^2+35576 \text{lam0}+42092\right)-70804\right) \text{dmu}^2-3 (\text{lam0}+2)^2 \left(\text{drho} \left(505 \text{lam0}^2+4024 \text{lam0}+6778\right)-5 \left(151 \text{lam0}^2+604 \text{lam0}+778\right)\right) \text{dmu}-45 \text{drho} (\text{lam0}+2)^3 (27 \text{lam0}+77)+\text{dlam}^2 \left(342 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^3+\left(3147 \text{lam0}^2+13885 \text{lam0}-148 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+17126\right) \text{dmu}^2-3 \left(-755 \text{lam0}^2-3632 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+2224 \text{lam0}+2428\right)-4664\right) \text{dmu}-9 (\text{lam0}+2) (\text{drho} (135 \text{lam0}+298)-105)\right)+\text{dlam} \left(936 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^4+\left(2052 \text{lam0}^3+22398 \text{lam0}^2+70048 \text{lam0}-466 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+72968\right) \text{dmu}^3+\left(6294 \text{lam0}^3+46637 \text{lam0}^2+120308 \text{lam0}-\text{drho} \left(888 \text{lam0}^3+11359 \text{lam0}^2+37012 \text{lam0}+35692\right)+108596\right) \text{dmu}^2-6 (\text{lam0}+2) \left(-755 \text{lam0}^2-3326 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+3124 \text{lam0}+4402\right)-4532\right) \text{dmu}-9 (\text{lam0}+2)^2 (\text{drho} (270 \text{lam0}+683)-105)\right)\right)}{6 (\text{dmu}+1) (\text{drho}+1)^2 \sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30)^2 \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)^2}\right) \text{rvar}^3+\frac{2}{15} \sqrt{\frac{\text{drho}+1}{\text{dlam}+2 \text{dmu}+\text{lam0}+2}} \left(-\frac{525 (\text{dmu}+1)^2 \left(-\frac{5 i (\text{lam0}+2) (\text{drho}+1)^4}{1386 (\text{dmu}+1)^3}+\frac{8 \left(-72 i \left(-\frac{1}{72 \sqrt{\text{lam0}+2}}-\frac{1}{168 (\text{lam0}+2)^{3/2}}-\frac{1}{504 (\text{lam0}+2)^{5/2}}+\frac{1}{1512 (\text{lam0}+2)^{7/2}}\right) (\text{lam0}+2)^{3/2}+i (3 \text{lam0}-2) \left(-\frac{1}{12 \sqrt{\text{lam0}+2}}+\frac{1}{14 (\text{lam0}+2)^{3/2}}-\frac{1}{28 (\text{lam0}+2)^{5/2}}\right) \sqrt{\text{lam0}+2}-\frac{1}{2} i (5 \text{lam0}+10)-144 i \left(\frac{\text{lam0}}{112 (\text{lam0}+2)^3}-\frac{1}{112 (\text{lam0}+2)^2}+\frac{1}{56 (\text{lam0}+2)^3}\right)-12 i \left(-\frac{\text{lam0}}{8 (\text{lam0}+2)^2}+\frac{11}{24 (\text{lam0}+2)}-\frac{2}{6 \text{lam0}+12}-\frac{1}{4 (\text{lam0}+2)^2}\right)-\frac{4 i \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right)}{3 \sqrt{\text{lam0}+2}}-\frac{5 i}{4 (\text{lam0}+2)}+\frac{i (-\text{lam0}-1) \text{lam0}}{2 (\text{lam0}+2)^2}-\frac{i (3 \text{lam0}+5) (3 \text{lam0}+8)}{42 (\text{lam0}+2)^2}-\frac{i (-7 \text{lam0}-12) \left(3 \text{lam0}^2+13 \text{lam0}+16\right)}{14 (\text{lam0}+2)^2}\right) (\text{drho}+1)^2}{105 (\text{dmu}+1)}+\frac{5 i (6 \text{drho} \text{dmu}-\text{dmu}-7) (\text{lam0}+2) (\text{drho}+1)}{168 (\text{dmu}+1)^2}+\frac{i \text{dmu} (\text{lam0}+2) (\text{drho}+1)}{12 (\text{dmu}+1)}+\frac{i (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) (\text{drho}+1)}{5940 (\text{dmu}+1) (\text{lam0}+2)^3}-\frac{i \left(14 \text{lam0} \text{dmu}^2+14 \text{dmu}^2-60 \text{drho} \text{dmu}-30 \text{drho} \text{lam0} \text{dmu}+19 \text{lam0} \text{dmu}+24 \text{dmu}+35 \text{lam0}+70\right) (\text{drho}+1)}{210 (\text{dmu}+1)^2 (\text{lam0}+2)^3}-\frac{i \left(42 \text{lam0}^2 \text{dmu}^2+182 \text{lam0} \text{dmu}^2+224 \text{dmu}^2-18 \text{drho} \text{lam0}^2 \text{dmu}+45 \text{lam0}^2 \text{dmu}-96 \text{drho} \text{dmu}-84 \text{drho} \text{lam0} \text{dmu}+217 \text{lam0} \text{dmu}+282 \text{dmu}-21 \text{drho} \text{lam0}^2-84 \text{drho}-84 \text{drho} \text{lam0}+35 \text{lam0}+70\right) (\text{drho}+1)}{1764 (\text{dmu}+1)^2 (\text{lam0}+2)^3}-\frac{2}{5} \left(-72 i \left(\frac{1}{576 \sqrt{\text{lam0}+2}}+\frac{5}{1008 (\text{lam0}+2)^{3/2}}+\frac{1}{2016 (\text{lam0}+2)^{5/2}}+\frac{1}{33264 (\text{lam0}+2)^{7/2}}-\frac{1}{133056 (\text{lam0}+2)^{9/2}}\right) (\text{lam0}+2)^{3/2}+i (3 \text{lam0}-2) \left(-\frac{1}{72 \sqrt{\text{lam0}+2}}-\frac{1}{168 (\text{lam0}+2)^{3/2}}-\frac{1}{504 (\text{lam0}+2)^{5/2}}+\frac{1}{1512 (\text{lam0}+2)^{7/2}}\right) \sqrt{\text{lam0}+2}+\frac{1}{24} i (5 \text{lam0}+10)-144 i \left(-\frac{\text{lam0}}{4032 (\text{lam0}+2)^4}+\frac{1}{4032 (\text{lam0}+2)^3}-\frac{1}{2016 (\text{lam0}+2)^4}\right)-12 i \left(\frac{\text{lam0}}{112 (\text{lam0}+2)^3}-\frac{1}{112 (\text{lam0}+2)^2}+\frac{1}{56 (\text{lam0}+2)^3}\right)+\frac{i \text{lam0} \left(-\frac{1}{12 \sqrt{\text{lam0}+2}}+\frac{1}{14 (\text{lam0}+2)^{3/2}}-\frac{1}{28 (\text{lam0}+2)^{5/2}}\right)}{2 \sqrt{\text{lam0}+2}}-\frac{4 i \left(\frac{1}{16} (\text{lam0}+2)^{3/2}-\frac{5 \sqrt{\text{lam0}+2}}{48}+\frac{1}{16 \sqrt{\text{lam0}+2}}-\frac{1}{16 (\text{lam0}+2)^{3/2}}+\frac{1}{24 (\text{lam0}+2)^{5/2}}\right)}{3 \sqrt{\text{lam0}+2}}-\frac{i (-7 \text{lam0}-12) \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right)}{21 (\text{lam0}+2)^{3/2}}+\frac{i (-\text{lam0}-1)}{8 (\text{lam0}+2)^2}+\frac{5 i (3 \text{lam0}+5)}{72 (\text{lam0}+2)^3}+\frac{i (3 \text{lam0}+8) (33 \text{lam0}+56)}{16632 (\text{lam0}+2)^3}-\frac{i (3 \text{lam0}+5) \left(3 \text{lam0}^2+13 \text{lam0}+16\right)}{84 (\text{lam0}+2)^3}\right) (\text{drho}+1)+\frac{i \text{dmu} (\text{drho}+1)}{18 (\text{dmu}+1) (\text{lam0}+2)^3}-24 i (\text{lam0}+2)^{3/2} \left(\frac{(-\text{lam0}-1) (\text{drho}+1)^4}{5940 (\text{dmu}+1)^3 (\text{lam0}+2)^{3/2}}-\frac{\left(-\frac{1}{12 \sqrt{\text{lam0}+2}}+\frac{1}{14 (\text{lam0}+2)^{3/2}}-\frac{1}{28 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)^3}{126 (\text{dmu}+1)^2}+\frac{6 \left(-\frac{1}{72 \sqrt{\text{lam0}+2}}-\frac{1}{168 (\text{lam0}+2)^{3/2}}-\frac{1}{504 (\text{lam0}+2)^{5/2}}+\frac{1}{1512 (\text{lam0}+2)^{7/2}}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}-\frac{(26 \text{lam0}+45) (\text{drho}+1)}{432432 (\text{dmu}+1) (\text{lam0}+2)^{9/2}}+\frac{(\text{dmu}-\text{drho}) (33 \text{lam0}+56) (\text{drho}+1)}{83160 (\text{dmu}+1)^2 (\text{lam0}+2)^{7/2}}+\frac{(3 \text{dmu}+4 \text{drho}+2 \text{drho} \text{lam0}+2 \text{lam0}+7) (\text{drho}+1)}{630 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}-\frac{4}{5} \left(\frac{1}{576 \sqrt{\text{lam0}+2}}+\frac{5}{1008 (\text{lam0}+2)^{3/2}}+\frac{1}{2016 (\text{lam0}+2)^{5/2}}+\frac{1}{33264 (\text{lam0}+2)^{7/2}}-\frac{1}{133056 (\text{lam0}+2)^{9/2}}\right) (\text{drho}+1)+\frac{i \left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) (3 \text{lam0}+5)}{126 (\text{lam0}+2)^{5/2}}+144 i \left(\frac{i (\text{drho}+1)^4}{698544 (\text{dmu}+1)^4 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)^3}{317520 (\text{dmu}+1)^3 (\text{lam0}+2)^{5/2}}+\frac{17 i (\text{drho}+1)^2}{10478160 (\text{dmu}+1)^2 (\text{lam0}+2)^{7/2}}+\frac{i (\text{drho}+1)}{6486480 (\text{dmu}+1) (\text{lam0}+2)^{9/2}}\right)+12 i \left(-\frac{i (\text{drho}+1)^4}{124740 (\text{dmu}+1)^4 \sqrt{\text{lam0}+2}}-\frac{11 i (\text{drho}+1)^3}{158760 (\text{dmu}+1)^3 (\text{lam0}+2)^{3/2}}-\frac{i (\text{drho}+1)^2}{13230 (\text{dmu}+1)^2 (\text{lam0}+2)^{5/2}}-\frac{i (\text{drho}+1)}{83160 (\text{dmu}+1) (\text{lam0}+2)^{7/2}}\right)+\frac{4 i \left(-\frac{i (\text{drho}+1)^4}{18480 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{2016 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{960 (\text{dmu}+1)}\right)}{3 \sqrt{\text{lam0}+2}}+\frac{i \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) (-7 \text{lam0}-12)}{21 (\text{lam0}+2)^{3/2}}\right)-2 i \sqrt{\text{lam0}+2} \left(-\frac{(\text{drho}+1)^4}{594 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}-\frac{(-\text{lam0}-1) (\text{drho}+1)^3}{126 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{6 \left(-\frac{1}{12 \sqrt{\text{lam0}+2}}+\frac{1}{14 (\text{lam0}+2)^{3/2}}-\frac{1}{28 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}-\frac{(\text{dmu}-\text{drho}) (3 \text{lam0}+5) (\text{drho}+1)}{210 (\text{dmu}+1)^2 (\text{lam0}+2)^{5/2}}+\frac{(33 \text{lam0}+56) (\text{drho}+1)}{8316 (\text{dmu}+1) (\text{lam0}+2)^{7/2}}-\frac{4}{5} \left(-\frac{1}{72 \sqrt{\text{lam0}+2}}-\frac{1}{168 (\text{lam0}+2)^{3/2}}-\frac{1}{504 (\text{lam0}+2)^{5/2}}+\frac{1}{1512 (\text{lam0}+2)^{7/2}}\right) (\text{drho}+1)+12 i \left(\frac{i (\text{drho}+1)^3}{2835 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}+\frac{i (\text{drho}+1)^2}{630 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)}{1890 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}\right)+144 i \left(-\frac{i (\text{drho}+1)^4}{124740 (\text{dmu}+1)^4 \sqrt{\text{lam0}+2}}-\frac{11 i (\text{drho}+1)^3}{158760 (\text{dmu}+1)^3 (\text{lam0}+2)^{3/2}}-\frac{i (\text{drho}+1)^2}{13230 (\text{dmu}+1)^2 (\text{lam0}+2)^{5/2}}-\frac{i (\text{drho}+1)}{83160 (\text{dmu}+1) (\text{lam0}+2)^{7/2}}\right)+\frac{4 i \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right)}{3 \sqrt{\text{lam0}+2}}+\frac{i \left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) (-7 \text{lam0}-12)}{21 (\text{lam0}+2)^{3/2}}\right)+\frac{2 i \left(\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (\text{drho}+1)^4}{1980 (\text{dmu}+1)^3}-\frac{\left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^3}{84 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}+\frac{6 \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}+\frac{(\text{lam0}+2)^{3/2} (\text{drho}+1)}{8 (\text{dmu}+1)}+\frac{(3 \text{lam0}+5) (\text{drho}+1)}{24 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}+\frac{3 \sqrt{\text{lam0}+2} (-\text{dmu}+2 \text{drho}+\text{drho} \text{lam0}+\text{lam0}+1) (\text{drho}+1)}{20 (\text{dmu}+1)^2}-\frac{4}{5} \left(\frac{1}{16} (\text{lam0}+2)^{3/2}-\frac{5 \sqrt{\text{lam0}+2}}{48}+\frac{1}{16 \sqrt{\text{lam0}+2}}-\frac{1}{16 (\text{lam0}+2)^{3/2}}+\frac{1}{24 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)-\frac{3 (\text{dmu}-\text{drho}) (\text{drho}+1)}{40 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-72 i \left(-\frac{i (\text{drho}+1)^4}{18480 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{2016 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{960 (\text{dmu}+1)}\right) (\text{lam0}+2)^{3/2}+i \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) \sqrt{\text{lam0}+2} (3 \text{lam0}-2)-144 i \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{66528 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{60480 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}+\frac{i (\text{drho}+1)^2}{1120 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)}{1920 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}\right)-12 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right)+\frac{i \left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) \text{lam0}}{2 \sqrt{\text{lam0}+2}}\right)}{3 \sqrt{\text{lam0}+2}}-\frac{i \left(-\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (\text{drho}+1)^3}{42 (\text{dmu}+1)^2}+\frac{9 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^2}{35 (\text{dmu}+1) \sqrt{\text{lam0}+2}}-\frac{3 (\text{lam0}+2)^{3/2} (\text{drho}+1)}{2 (\text{dmu}+1)}-\frac{4}{5} \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)-\frac{3 (\text{dmu}-\text{drho}) \text{lam0} (\text{drho}+1)}{10 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{3 (\text{drho}+1)}{4 (\text{dmu}+1) \sqrt{\text{lam0}+2}}-72 i \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) (\text{lam0}+2)^{3/2}+i \left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) \sqrt{\text{lam0}+2} (3 \text{lam0}-2)-12 i \left(-\frac{3 i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dmu}+1)^3}+\frac{11 i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{420 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{40 (\text{dmu}+1) \sqrt{\text{lam0}+2}}\right)-144 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right)\right)}{7 (\text{lam0}+2)^{3/2}}-\frac{1}{2} i \left(-\frac{(5 \text{lam0}+10) (\text{drho}+1)^3}{126 (\text{dmu}+1)^2}+\frac{(3 \text{lam0}+5) (\text{drho}+1)}{42 (\text{dmu}+1) (\text{lam0}+2)}-\frac{(-7 \text{lam0}-12) (-\text{dmu}+2 \text{drho}+\text{drho} \text{lam0}+\text{lam0}+1) (\text{drho}+1)}{70 (\text{dmu}+1)^2 (\text{lam0}+2)}+\frac{\left(9 \text{lam0} \text{dmu}^2+6 \text{dmu}^2+6 \text{drho} \text{lam0}^2 \text{dmu}+6 \text{lam0}^2 \text{dmu}+64 \text{drho} \text{dmu}+44 \text{drho} \text{lam0} \text{dmu}+62 \text{lam0} \text{dmu}+76 \text{dmu}+32 \text{drho}^2+8 \text{drho}^2 \text{lam0}^2+22 \text{drho} \text{lam0}^2+14 \text{lam0}^2+128 \text{drho}+32 \text{drho}^2 \text{lam0}+108 \text{drho} \text{lam0}+85 \text{lam0}+102\right) (\text{drho}+1)}{315 (\text{dmu}+1)^3 (\text{lam0}+2)}-\frac{4}{5} \left(-\frac{\text{lam0}}{8 (\text{lam0}+2)^2}+\frac{11}{24 (\text{lam0}+2)}-\frac{2}{6 \text{lam0}+12}-\frac{1}{4 (\text{lam0}+2)^2}\right) (\text{drho}+1)+\frac{4 i \left(-\frac{3 i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dmu}+1)^3}+\frac{11 i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{420 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{40 (\text{dmu}+1) \sqrt{\text{lam0}+2}}\right)}{3 \sqrt{\text{lam0}+2}}\right)-3 i \left(\frac{(5 \text{lam0}+10) (\text{drho}+1)^4}{5940 (\text{dmu}+1)^3}+\frac{6 \left(-\frac{\text{lam0}}{8 (\text{lam0}+2)^2}+\frac{11}{24 (\text{lam0}+2)}-\frac{2}{6 \text{lam0}+12}-\frac{1}{4 (\text{lam0}+2)^2}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}-\frac{(33 \text{lam0}+56) (\text{drho}+1)}{16632 (\text{dmu}+1) (\text{lam0}+2)^2}-\frac{(3 \text{lam0}+5) (-\text{dmu}+2 \text{drho}+\text{drho} \text{lam0}+\text{lam0}+1) (\text{drho}+1)}{420 (\text{dmu}+1)^2 (\text{lam0}+2)^2}+\frac{\text{lam0} (3 \text{dmu}+4 \text{drho}+2 \text{drho} \text{lam0}+2 \text{lam0}+7) (\text{drho}+1)}{630 (\text{dmu}+1)^2 (\text{lam0}+2)^2}-\frac{4}{5} \left(\frac{\text{lam0}}{112 (\text{lam0}+2)^3}-\frac{1}{112 (\text{lam0}+2)^2}+\frac{1}{56 (\text{lam0}+2)^3}\right) (\text{drho}+1)+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(\frac{i (\text{drho}+1)^3}{2835 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}+\frac{i (\text{drho}+1)^2}{630 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)}{1890 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}\right)-72 i (\text{lam0}+2)^{3/2} \left(-\frac{i (\text{drho}+1)^4}{124740 (\text{dmu}+1)^4 \sqrt{\text{lam0}+2}}-\frac{11 i (\text{drho}+1)^3}{158760 (\text{dmu}+1)^3 (\text{lam0}+2)^{3/2}}-\frac{i (\text{drho}+1)^2}{13230 (\text{dmu}+1)^2 (\text{lam0}+2)^{5/2}}-\frac{i (\text{drho}+1)}{83160 (\text{dmu}+1) (\text{lam0}+2)^{7/2}}\right)+\frac{i (-7 \text{lam0}-12) \left(-\frac{3 i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dmu}+1)^3}+\frac{11 i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{420 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{40 (\text{dmu}+1) \sqrt{\text{lam0}+2}}\right)}{21 (\text{lam0}+2)^{3/2}}+\frac{4 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right)}{3 \sqrt{\text{lam0}+2}}\right)-48 i \left(-\frac{\left(-\frac{\text{lam0}}{8 (\text{lam0}+2)^2}+\frac{11}{24 (\text{lam0}+2)}-\frac{2}{6 \text{lam0}+12}-\frac{1}{4 (\text{lam0}+2)^2}\right) (\text{drho}+1)^3}{126 (\text{dmu}+1)^2}+\frac{6 \left(\frac{\text{lam0}}{112 (\text{lam0}+2)^3}-\frac{1}{112 (\text{lam0}+2)^2}+\frac{1}{56 (\text{lam0}+2)^3}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}+\frac{(26 \text{lam0}+45) (\text{drho}+1)}{864864 (\text{dmu}+1) (\text{lam0}+2)^3}+\frac{(33 \text{lam0}+56) (-\text{dmu}+2 \text{drho}+\text{drho} \text{lam0}+\text{lam0}+1) (\text{drho}+1)}{166320 (\text{dmu}+1)^2 (\text{lam0}+2)^3}+\frac{(3 \text{dmu}+4 \text{drho}+2 \text{drho} \text{lam0}+2 \text{lam0}+7) (\text{drho}+1)}{2520 (\text{dmu}+1)^2 (\text{lam0}+2)^2}-\frac{4}{5} \left(-\frac{\text{lam0}}{4032 (\text{lam0}+2)^4}+\frac{1}{4032 (\text{lam0}+2)^3}-\frac{1}{2016 (\text{lam0}+2)^4}\right) (\text{drho}+1)-72 i (\text{lam0}+2)^{3/2} \left(\frac{i (\text{drho}+1)^4}{698544 (\text{dmu}+1)^4 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)^3}{317520 (\text{dmu}+1)^3 (\text{lam0}+2)^{5/2}}+\frac{17 i (\text{drho}+1)^2}{10478160 (\text{dmu}+1)^2 (\text{lam0}+2)^{7/2}}+\frac{i (\text{drho}+1)}{6486480 (\text{dmu}+1) (\text{lam0}+2)^{9/2}}\right)+\frac{i \text{lam0} \left(\frac{i (\text{drho}+1)^3}{2835 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}+\frac{i (\text{drho}+1)^2}{630 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)}{1890 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}\right)}{2 \sqrt{\text{lam0}+2}}+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(-\frac{i (\text{drho}+1)^4}{124740 (\text{dmu}+1)^4 \sqrt{\text{lam0}+2}}-\frac{11 i (\text{drho}+1)^3}{158760 (\text{dmu}+1)^3 (\text{lam0}+2)^{3/2}}-\frac{i (\text{drho}+1)^2}{13230 (\text{dmu}+1)^2 (\text{lam0}+2)^{5/2}}-\frac{i (\text{drho}+1)}{83160 (\text{dmu}+1) (\text{lam0}+2)^{7/2}}\right)+\frac{4 i \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{66528 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{60480 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}+\frac{i (\text{drho}+1)^2}{1120 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)}{1920 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}\right)}{3 \sqrt{\text{lam0}+2}}+\frac{i (3 \text{lam0}+5) \left(-\frac{3 i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dmu}+1)^3}+\frac{11 i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{420 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{40 (\text{dmu}+1) \sqrt{\text{lam0}+2}}\right)}{126 (\text{lam0}+2)^{5/2}}+\frac{i (-7 \text{lam0}-12) \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right)}{21 (\text{lam0}+2)^{3/2}}\right)\right) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}{(\text{drho}+1)^3 \sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)}+\frac{75 i \left(14 \text{dmu}^2-2 \text{drho} \text{dmu}+33 \text{dmu}+14 \text{drho}+35\right) \sqrt{\text{lam0}+2} \left(\frac{525 (\text{dmu}+1)^2 \left(-\frac{\sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) (\text{drho}+1)^5}{20790 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{\sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) (\text{drho}+1)^5}{29700 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{\left(42 \text{lam0}^2 \text{dmu}^2+182 \text{lam0} \text{dmu}^2+224 \text{dmu}^2-18 \text{drho} \text{lam0}^2 \text{dmu}+45 \text{lam0}^2 \text{dmu}-96 \text{drho} \text{dmu}-84 \text{drho} \text{lam0} \text{dmu}+217 \text{lam0} \text{dmu}+282 \text{dmu}-21 \text{drho} \text{lam0}^2-84 \text{drho}-84 \text{drho} \text{lam0}+35 \text{lam0}+70\right) (\text{drho}+1)^4}{8820 (\text{dmu}+1) \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{17 \left(-42 \text{dmu} \text{lam0}^3+21 \text{drho} \text{lam0}^3-84 \text{dmu}^2 \text{lam0}^2-42 \text{dlam} \text{dmu} \text{lam0}^2-272 \text{dmu} \text{lam0}^2+21 \text{dlam} \text{drho} \text{lam0}^2+36 \text{dmu} \text{drho} \text{lam0}^2+161 \text{drho} \text{lam0}^2-364 \text{dmu}^2 \text{lam0}-21 \text{dlam} \text{lam0}-182 \text{dlam} \text{dmu} \text{lam0}-658 \text{dmu} \text{lam0}+98 \text{dlam} \text{drho} \text{lam0}+168 \text{dmu} \text{drho} \text{lam0}+392 \text{drho} \text{lam0}-448 \text{dmu}^2-42 \text{dlam}-224 \text{dlam} \text{dmu}-564 \text{dmu}+112 \text{dlam} \text{drho}+192 \text{dmu} \text{drho}+308 \text{drho}\right) (\text{drho}+1)^4}{132300 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{lam0}+2)^{3/2} \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right) (\text{drho}+1)^3}{840 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{\left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right) (\text{drho}+1)^3}{1050 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{\sqrt{\text{lam0}+2} \left(-28 \text{dmu}^4-76 \text{dlam} \text{dmu}^3+220 \text{drho} \text{dmu}^3+110 \text{drho} \text{lam0} \text{dmu}^3+118 \text{lam0} \text{dmu}^3+52 \text{dmu}^3-31 \text{dlam}^2 \text{dmu}^2+161 \text{drho} \text{lam0}^2 \text{dmu}^2+196 \text{lam0}^2 \text{dmu}^2+50 \text{dlam} \text{dmu}^2+322 \text{dlam} \text{drho} \text{dmu}^2+1124 \text{drho} \text{dmu}^2+165 \text{dlam} \text{lam0} \text{dmu}^2+161 \text{dlam} \text{drho} \text{lam0} \text{dmu}^2+884 \text{drho} \text{lam0} \text{dmu}^2+864 \text{lam0} \text{dmu}^2+788 \text{dmu}^2+47 \text{drho} \text{lam0}^3 \text{dmu}+59 \text{lam0}^3 \text{dmu}+24 \text{dlam}^2 \text{dmu}+118 \text{dlam} \text{lam0}^2 \text{dmu}+94 \text{dlam} \text{drho} \text{lam0}^2 \text{dmu}+552 \text{drho} \text{lam0}^2 \text{dmu}+614 \text{lam0}^2 \text{dmu}+600 \text{dlam} \text{dmu}+94 \text{dlam}^2 \text{drho} \text{dmu}+916 \text{dlam} \text{drho} \text{dmu}+1588 \text{drho} \text{dmu}+59 \text{dlam}^2 \text{lam0} \text{dmu}+638 \text{dlam} \text{lam0} \text{dmu}+47 \text{dlam}^2 \text{drho} \text{lam0} \text{dmu}+646 \text{dlam} \text{drho} \text{lam0} \text{dmu}+1710 \text{drho} \text{lam0} \text{dmu}+1646 \text{lam0} \text{dmu}+1308 \text{dmu}+63 \text{drho} \text{lam0}^3+75 \text{lam0}^3+87 \text{dlam}^2+150 \text{dlam} \text{lam0}^2+126 \text{dlam} \text{drho} \text{lam0}^2+423 \text{drho} \text{lam0}^2+450 \text{lam0}^2+474 \text{dlam}+126 \text{dlam}^2 \text{drho}+594 \text{dlam} \text{drho}+684 \text{drho}+75 \text{dlam}^2 \text{lam0}+537 \text{dlam} \text{lam0}+63 \text{dlam}^2 \text{drho} \text{lam0}+549 \text{dlam} \text{drho} \text{lam0}+936 \text{drho} \text{lam0}+900 \text{lam0}+600\right) (\text{drho}+1)^3}{6300 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{dmu}+1) \left(-\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (\text{drho}+1)^3}{42 (\text{dmu}+1)^2}+\frac{9 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^2}{35 (\text{dmu}+1) \sqrt{\text{lam0}+2}}-\frac{3 (\text{lam0}+2)^{3/2} (\text{drho}+1)}{2 (\text{dmu}+1)}-\frac{4}{5} \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)-\frac{3 (\text{dmu}-\text{drho}) \text{lam0} (\text{drho}+1)}{10 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{3 (\text{drho}+1)}{4 (\text{dmu}+1) \sqrt{\text{lam0}+2}}-72 i \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) (\text{lam0}+2)^{3/2}+i \left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) \sqrt{\text{lam0}+2} (3 \text{lam0}-2)-12 i \left(-\frac{3 i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dmu}+1)^3}+\frac{11 i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{420 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{40 (\text{dmu}+1) \sqrt{\text{lam0}+2}}\right)-144 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right)\right) (\text{drho}+1)^2}{35 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{8 \left(-\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (\text{drho}+1)^3}{210 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^2}{70 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{4 (\text{dmu}+1) \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{(\text{lam0}+2)^{3/2} (\text{drho}+1)}{2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{\text{drho}+1}{4 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) \text{lam0} (\text{drho}+1)}{10 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-72 i (\text{lam0}+2)^{3/2} \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-144 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-12 i \left(-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{84 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{60 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{120 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right) (\text{drho}+1)^2}{105 (\text{dmu}+1)}+\frac{2 (\text{dmu}+1) \left(\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (\text{drho}+1)^4}{1980 (\text{dmu}+1)^3}-\frac{\left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^3}{84 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}+\frac{6 \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}+\frac{(\text{lam0}+2)^{3/2} (\text{drho}+1)}{8 (\text{dmu}+1)}+\frac{(3 \text{lam0}+5) (\text{drho}+1)}{24 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}+\frac{3 \sqrt{\text{lam0}+2} (-\text{dmu}+2 \text{drho}+\text{drho} \text{lam0}+\text{lam0}+1) (\text{drho}+1)}{20 (\text{dmu}+1)^2}-\frac{4}{5} \left(\frac{1}{16} (\text{lam0}+2)^{3/2}-\frac{5 \sqrt{\text{lam0}+2}}{48}+\frac{1}{16 \sqrt{\text{lam0}+2}}-\frac{1}{16 (\text{lam0}+2)^{3/2}}+\frac{1}{24 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)-\frac{3 (\text{dmu}-\text{drho}) (\text{drho}+1)}{40 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-72 i \left(-\frac{i (\text{drho}+1)^4}{18480 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{2016 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{960 (\text{dmu}+1)}\right) (\text{lam0}+2)^{3/2}+i \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) \sqrt{\text{lam0}+2} (3 \text{lam0}-2)-144 i \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{66528 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{60480 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}+\frac{i (\text{drho}+1)^2}{1120 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)}{1920 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}\right)-12 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right)+\frac{i \left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) \text{lam0}}{2 \sqrt{\text{lam0}+2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{2}{5} \left(\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^4}{83160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) \left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^3}{420 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(3 \text{lam0}+5) (\text{drho}+1)}{72 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{\sqrt{\text{lam0}+2} (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{4 (\text{dmu}+1) \left(\frac{1}{16} (\text{lam0}+2)^{3/2}-\frac{5 \sqrt{\text{lam0}+2}}{48}+\frac{1}{16 \sqrt{\text{lam0}+2}}-\frac{1}{16 (\text{lam0}+2)^{3/2}}+\frac{1}{24 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(\text{lam0}+2)^{3/2} (\text{drho}+1)}{24 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (\text{drho}+1)}{40 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-72 i (\text{lam0}+2)^{3/2} \left(-\frac{i (\text{drho}+1)^4}{166320 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{10080 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{1008 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{1440 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{2880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+\frac{i \text{lam0} \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{2 \sqrt{\text{lam0}+2}}-144 i \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{110880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{12096 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{10080 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{8640 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{3456 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-12 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right) (\text{drho}+1)-24 i (\text{lam0}+2)^{3/2} \left(-\frac{i (\text{dmu}-\text{drho}) (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^5}{415800 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (\text{drho}+1)^5}{29700 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (2 \text{dlam}+7 \text{dmu}+2 \text{lam0}+7) (\text{drho}+1)^3}{3150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{\left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (\text{drho}+1)^3}{630 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{\left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^3}{126 (\text{dmu}+1)^2}-\frac{\left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) (7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{6 \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}-\frac{4}{5} \left(-\frac{i (\text{drho}+1)^4}{166320 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{10080 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{1008 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{1440 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{2880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)+\frac{4 (\text{dmu}+1) \left(-\frac{i (\text{drho}+1)^4}{18480 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{2016 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{960 (\text{dmu}+1)}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+144 i \left(\frac{(\text{drho}+1)^6}{3492720 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(\text{drho}+1)^6}{1587600 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{17 (\text{drho}+1)^6}{52390800 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}\right)+12 i \left(-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{11 (\text{drho}+1)^5}{793800 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{drho}+1)^5}{66150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(-\text{drho}-1) (\text{drho}+1)^4}{1247400 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)-2 i \sqrt{\text{lam0}+2} \left(-\frac{i (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^5}{41580 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^5}{2970 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (\text{dmu}-\text{drho}) (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (\text{drho}+1)^4}{1050 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{2 i \left(2 \text{dlam}^2+17 \text{dmu} \text{dlam}+4 \text{lam0} \text{dlam}+17 \text{dlam}+29 \text{dmu}^2+2 \text{lam0}^2+58 \text{dmu}+17 \text{dmu} \text{lam0}+17 \text{lam0}+29\right) (\text{drho}+1)^4}{4725 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (\text{drho}+1)^4}{630 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{\left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) (7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{6 \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}-\frac{4}{5} \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)+\frac{4 (\text{dmu}+1) \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+144 i \left(-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{11 (\text{drho}+1)^5}{793800 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{drho}+1)^5}{66150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(-\text{drho}-1) (\text{drho}+1)^4}{1247400 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)-3 i \left(\frac{i (\text{lam0}+2)^{3/2} (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^5}{83160 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^5}{5940 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i \sqrt{\text{lam0}+2} (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)^4}{1260 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i \sqrt{\text{lam0}+2} (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (-\text{dmu}+2 \text{drho}+\text{drho} \text{lam0}+\text{lam0}+1) (\text{drho}+1)^4}{2100 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(2 \text{dlam}^2+17 \text{dmu} \text{dlam}+4 \text{lam0} \text{dlam}+17 \text{dlam}+29 \text{dmu}^2+2 \text{lam0}^2+58 \text{dmu}+17 \text{dmu} \text{lam0}+17 \text{lam0}+29\right) (\text{drho}+1)^4}{28350 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i \text{lam0} (2 \text{dlam}+7 \text{dmu}+2 \text{lam0}+7) (\text{drho}+1)^3}{3150 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(-\frac{3 i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dmu}+1)^3}+\frac{11 i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{420 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{40 (\text{dmu}+1) \sqrt{\text{lam0}+2}}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{6 \left(-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{84 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{60 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{120 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}+\frac{4 (\text{dmu}+1) \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{4}{5} \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)-72 i (\text{lam0}+2)^{3/2} \left(-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{11 (\text{drho}+1)^5}{793800 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{drho}+1)^5}{66150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(-\text{drho}-1) (\text{drho}+1)^4}{1247400 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)-48 i \left(\frac{i \sqrt{\text{lam0}+2} (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)^5}{59400 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i \sqrt{\text{lam0}+2} (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (-\text{dmu}+2 \text{drho}+\text{drho} \text{lam0}+\text{lam0}+1) (\text{drho}+1)^5}{831600 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i \text{lam0} \left(2 \text{dlam}^2+17 \text{dmu} \text{dlam}+4 \text{lam0} \text{dlam}+17 \text{dlam}+29 \text{dmu}^2+2 \text{lam0}^2+58 \text{dmu}+17 \text{dmu} \text{lam0}+17 \text{lam0}+29\right) (\text{drho}+1)^4}{56700 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (2 \text{dlam}+7 \text{dmu}+2 \text{lam0}+7) (\text{drho}+1)^3}{12600 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) \left(-\frac{3 i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dmu}+1)^3}+\frac{11 i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{420 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{40 (\text{dmu}+1) \sqrt{\text{lam0}+2}}\right) (\text{drho}+1)^3}{630 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{\left(-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{84 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{60 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{120 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^3}{126 (\text{dmu}+1)^2}-\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{6 \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}+\frac{4 (\text{dmu}+1) \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{66528 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{60480 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}+\frac{i (\text{drho}+1)^2}{1120 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)}{1920 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{4}{5} \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{110880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{12096 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{10080 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{8640 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{3456 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)-72 i (\text{lam0}+2)^{3/2} \left(\frac{(\text{drho}+1)^6}{3492720 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(\text{drho}+1)^6}{1587600 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{17 (\text{drho}+1)^6}{52390800 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}\right)+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{11 (\text{drho}+1)^5}{793800 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{drho}+1)^5}{66150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(-\text{drho}-1) (\text{drho}+1)^4}{1247400 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)\right) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}{(\text{drho}+1)^3 \sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)}+\frac{\left(504 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^5+4 \left(702 \text{lam0}^3+6036 \text{lam0}^2+16771 \text{lam0}-73 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+16094\right) \text{dmu}^4+2 \left(513 \text{lam0}^4+8976 \text{lam0}^3+45815 \text{lam0}^2+96812 \text{lam0}-\text{drho} \left(699 \text{lam0}^3+6745 \text{lam0}^2+19504 \text{lam0}+17620\right)+75620\right) \text{dmu}^3-(\text{lam0}+2) \left(-3147 \text{lam0}^3-26458 \text{lam0}^2-72604 \text{lam0}+\text{drho} \left(444 \text{lam0}^3+8399 \text{lam0}^2+35576 \text{lam0}+42092\right)-70804\right) \text{dmu}^2-3 (\text{lam0}+2)^2 \left(\text{drho} \left(505 \text{lam0}^2+4024 \text{lam0}+6778\right)-5 \left(151 \text{lam0}^2+604 \text{lam0}+778\right)\right) \text{dmu}-45 \text{drho} (\text{lam0}+2)^3 (27 \text{lam0}+77)+\text{dlam}^2 \left(342 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^3+\left(3147 \text{lam0}^2+13885 \text{lam0}-148 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+17126\right) \text{dmu}^2-3 \left(-755 \text{lam0}^2-3632 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+2224 \text{lam0}+2428\right)-4664\right) \text{dmu}-9 (\text{lam0}+2) (\text{drho} (135 \text{lam0}+298)-105)\right)+\text{dlam} \left(936 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^4+\left(2052 \text{lam0}^3+22398 \text{lam0}^2+70048 \text{lam0}-466 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+72968\right) \text{dmu}^3+\left(6294 \text{lam0}^3+46637 \text{lam0}^2+120308 \text{lam0}-\text{drho} \left(888 \text{lam0}^3+11359 \text{lam0}^2+37012 \text{lam0}+35692\right)+108596\right) \text{dmu}^2-6 (\text{lam0}+2) \left(-755 \text{lam0}^2-3326 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+3124 \text{lam0}+4402\right)-4532\right) \text{dmu}-9 (\text{lam0}+2)^2 (\text{drho} (270 \text{lam0}+683)-105)\right)\right)^2}{324 (\text{dmu}+1)^2 (\text{lam0}+2)^2 (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30)^2 \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}\right) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}{(\text{drho}+1)^2 (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)}-\frac{25 i \left(126 \text{dmu}^3-54 \text{drho} \text{dmu}^2+261 \text{dmu}^2-2 \text{drho}^2 \text{dmu}-103 \text{drho} \text{dmu}+151 \text{dmu}-18 \text{drho}^2-81 \text{drho}\right) \left(504 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^5+4 \left(702 \text{lam0}^3+6036 \text{lam0}^2+16771 \text{lam0}-73 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+16094\right) \text{dmu}^4+2 \left(513 \text{lam0}^4+8976 \text{lam0}^3+45815 \text{lam0}^2+96812 \text{lam0}-\text{drho} \left(699 \text{lam0}^3+6745 \text{lam0}^2+19504 \text{lam0}+17620\right)+75620\right) \text{dmu}^3-(\text{lam0}+2) \left(-3147 \text{lam0}^3-26458 \text{lam0}^2-72604 \text{lam0}+\text{drho} \left(444 \text{lam0}^3+8399 \text{lam0}^2+35576 \text{lam0}+42092\right)-70804\right) \text{dmu}^2-3 (\text{lam0}+2)^2 \left(\text{drho} \left(505 \text{lam0}^2+4024 \text{lam0}+6778\right)-5 \left(151 \text{lam0}^2+604 \text{lam0}+778\right)\right) \text{dmu}-45 \text{drho} (\text{lam0}+2)^3 (27 \text{lam0}+77)+\text{dlam}^2 \left(342 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^3+\left(3147 \text{lam0}^2+13885 \text{lam0}-148 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+17126\right) \text{dmu}^2-3 \left(-755 \text{lam0}^2-3632 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+2224 \text{lam0}+2428\right)-4664\right) \text{dmu}-9 (\text{lam0}+2) (\text{drho} (135 \text{lam0}+298)-105)\right)+\text{dlam} \left(936 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^4+\left(2052 \text{lam0}^3+22398 \text{lam0}^2+70048 \text{lam0}-466 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+72968\right) \text{dmu}^3+\left(6294 \text{lam0}^3+46637 \text{lam0}^2+120308 \text{lam0}-\text{drho} \left(888 \text{lam0}^3+11359 \text{lam0}^2+37012 \text{lam0}+35692\right)+108596\right) \text{dmu}^2-6 (\text{lam0}+2) \left(-755 \text{lam0}^2-3326 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+3124 \text{lam0}+4402\right)-4532\right) \text{dmu}-9 (\text{lam0}+2)^2 (\text{drho} (270 \text{lam0}+683)-105)\right)\right) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}{108 (\text{dmu}+1)^2 (\text{drho}+1)^2 \sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30)^2 \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)^2}\right) \text{rvar}\right)+\frac{6 \left(\frac{5 i (\text{drho}+1) \sqrt{\text{lam0}+2} \left(-28 \text{dmu}^2-14 \text{dlam} \text{dmu}+18 \text{drho} \text{dmu}-14 \text{lam0} \text{dmu}-52 \text{dmu}+21 \text{dlam} \text{drho}+42 \text{drho}+21 \text{drho} \text{lam0}\right) \text{rvar}^6}{1512 (\text{dmu}+1) (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)}-\frac{(\text{drho}+1)^2 \left(-\frac{25 i \sqrt{\text{lam0}+2} \left(168 \text{dmu}^3+12 (14 \text{dlam}-6 \text{drho}+14 \text{lam0}+29) \text{dmu}^2+2 \left(21 \text{dlam}^2+(-39 \text{drho}+42 \text{lam0}+87) \text{dlam}+5 \text{drho}^2+21 \text{lam0}^2-77 \text{drho}-39 \text{drho} \text{lam0}+87 \text{lam0}+86\right) \text{dmu}-3 \text{drho} (\text{dlam}+\text{lam0}+2) (7 \text{dlam}-3 \text{drho}+7 \text{lam0}+11)\right) (\text{dmu}+1)^2}{6 (\text{drho}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2) (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)}-\frac{25 i \left(-28 \text{dmu}^2-14 \text{dlam} \text{dmu}+18 \text{drho} \text{dmu}-14 \text{lam0} \text{dmu}-52 \text{dmu}+21 \text{dlam} \text{drho}+42 \text{drho}+21 \text{drho} \text{lam0}\right) \left(504 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^5+4 \left(702 \text{lam0}^3+6036 \text{lam0}^2+16771 \text{lam0}-73 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+16094\right) \text{dmu}^4+2 \left(513 \text{lam0}^4+8976 \text{lam0}^3+45815 \text{lam0}^2+96812 \text{lam0}-\text{drho} \left(699 \text{lam0}^3+6745 \text{lam0}^2+19504 \text{lam0}+17620\right)+75620\right) \text{dmu}^3-(\text{lam0}+2) \left(-3147 \text{lam0}^3-26458 \text{lam0}^2-72604 \text{lam0}+\text{drho} \left(444 \text{lam0}^3+8399 \text{lam0}^2+35576 \text{lam0}+42092\right)-70804\right) \text{dmu}^2-3 (\text{lam0}+2)^2 \left(\text{drho} \left(505 \text{lam0}^2+4024 \text{lam0}+6778\right)-5 \left(151 \text{lam0}^2+604 \text{lam0}+778\right)\right) \text{dmu}-45 \text{drho} (\text{lam0}+2)^3 (27 \text{lam0}+77)+\text{dlam}^2 \left(342 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^3+\left(3147 \text{lam0}^2+13885 \text{lam0}-148 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+17126\right) \text{dmu}^2-3 \left(-755 \text{lam0}^2-3632 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+2224 \text{lam0}+2428\right)-4664\right) \text{dmu}-9 (\text{lam0}+2) (\text{drho} (135 \text{lam0}+298)-105)\right)+\text{dlam} \left(936 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^4+\left(2052 \text{lam0}^3+22398 \text{lam0}^2+70048 \text{lam0}-466 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+72968\right) \text{dmu}^3+\left(6294 \text{lam0}^3+46637 \text{lam0}^2+120308 \text{lam0}-\text{drho} \left(888 \text{lam0}^3+11359 \text{lam0}^2+37012 \text{lam0}+35692\right)+108596\right) \text{dmu}^2-6 (\text{lam0}+2) \left(-755 \text{lam0}^2-3326 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+3124 \text{lam0}+4402\right)-4532\right) \text{dmu}-9 (\text{lam0}+2)^2 (\text{drho} (270 \text{lam0}+683)-105)\right)\right) (\text{dmu}+1)}{18 (\text{drho}+1)^2 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2) (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30)^2 \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)^2}\right) \text{rvar}^4}{210 (\text{dmu}+1)^2}+\frac{(\text{drho}+1) \left(-\frac{525 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2 \left(\frac{i (\text{dmu}+1) (\text{lam0}+2) (\text{drho}+1)^4}{1188 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(\text{dmu}+1) \left(-72 i \left(-\frac{1}{72 \sqrt{\text{lam0}+2}}-\frac{1}{168 (\text{lam0}+2)^{3/2}}-\frac{1}{504 (\text{lam0}+2)^{5/2}}+\frac{1}{1512 (\text{lam0}+2)^{7/2}}\right) (\text{lam0}+2)^{3/2}+i (3 \text{lam0}-2) \left(-\frac{1}{12 \sqrt{\text{lam0}+2}}+\frac{1}{14 (\text{lam0}+2)^{3/2}}-\frac{1}{28 (\text{lam0}+2)^{5/2}}\right) \sqrt{\text{lam0}+2}-\frac{1}{2} i (5 \text{lam0}+10)-144 i \left(\frac{\text{lam0}}{112 (\text{lam0}+2)^3}-\frac{1}{112 (\text{lam0}+2)^2}+\frac{1}{56 (\text{lam0}+2)^3}\right)-12 i \left(-\frac{\text{lam0}}{8 (\text{lam0}+2)^2}+\frac{11}{24 (\text{lam0}+2)}-\frac{2}{6 \text{lam0}+12}-\frac{1}{4 (\text{lam0}+2)^2}\right)-\frac{4 i \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right)}{3 \sqrt{\text{lam0}+2}}-\frac{5 i}{4 (\text{lam0}+2)}+\frac{i (-\text{lam0}-1) \text{lam0}}{2 (\text{lam0}+2)^2}-\frac{i (3 \text{lam0}+5) (3 \text{lam0}+8)}{42 (\text{lam0}+2)^2}-\frac{i (-7 \text{lam0}-12) \left(3 \text{lam0}^2+13 \text{lam0}+16\right)}{14 (\text{lam0}+2)^2}\right) (\text{drho}+1)^2}{35 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) (\text{drho}+1)}{17820 (\text{lam0}+2)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{5 i (\text{lam0}+2) (12 \text{drho} \text{dmu}-2 \text{dmu}+7 \text{dlam} \text{drho}+14 \text{drho}+7 \text{drho} \text{lam0}) (\text{drho}+1)}{504 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i \left(-28 \text{lam0} \text{dmu}^2-28 \text{dmu}^2-14 \text{lam0}^2 \text{dmu}-14 \text{dlam} \text{dmu}+120 \text{drho} \text{dmu}-14 \text{dlam} \text{lam0} \text{dmu}+60 \text{drho} \text{lam0} \text{dmu}-52 \text{lam0} \text{dmu}-48 \text{dmu}+35 \text{drho} \text{lam0}^2+70 \text{dlam} \text{drho}+140 \text{drho}+35 \text{dlam} \text{drho} \text{lam0}+140 \text{drho} \text{lam0}\right) (\text{drho}+1)}{630 (\text{lam0}+2)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i \left(-42 \text{dmu} \text{lam0}^3+21 \text{drho} \text{lam0}^3-84 \text{dmu}^2 \text{lam0}^2-42 \text{dlam} \text{dmu} \text{lam0}^2-272 \text{dmu} \text{lam0}^2+21 \text{dlam} \text{drho} \text{lam0}^2+36 \text{dmu} \text{drho} \text{lam0}^2+161 \text{drho} \text{lam0}^2-364 \text{dmu}^2 \text{lam0}-21 \text{dlam} \text{lam0}-182 \text{dlam} \text{dmu} \text{lam0}-658 \text{dmu} \text{lam0}+98 \text{dlam} \text{drho} \text{lam0}+168 \text{dmu} \text{drho} \text{lam0}+392 \text{drho} \text{lam0}-448 \text{dmu}^2-42 \text{dlam}-224 \text{dlam} \text{dmu}-564 \text{dmu}+112 \text{dlam} \text{drho}+192 \text{dmu} \text{drho}+308 \text{drho}\right) (\text{drho}+1)}{5292 (\text{lam0}+2)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{2 (\text{dmu}+1) \left(-72 i \left(\frac{1}{576 \sqrt{\text{lam0}+2}}+\frac{5}{1008 (\text{lam0}+2)^{3/2}}+\frac{1}{2016 (\text{lam0}+2)^{5/2}}+\frac{1}{33264 (\text{lam0}+2)^{7/2}}-\frac{1}{133056 (\text{lam0}+2)^{9/2}}\right) (\text{lam0}+2)^{3/2}+i (3 \text{lam0}-2) \left(-\frac{1}{72 \sqrt{\text{lam0}+2}}-\frac{1}{168 (\text{lam0}+2)^{3/2}}-\frac{1}{504 (\text{lam0}+2)^{5/2}}+\frac{1}{1512 (\text{lam0}+2)^{7/2}}\right) \sqrt{\text{lam0}+2}+\frac{1}{24} i (5 \text{lam0}+10)-144 i \left(-\frac{\text{lam0}}{4032 (\text{lam0}+2)^4}+\frac{1}{4032 (\text{lam0}+2)^3}-\frac{1}{2016 (\text{lam0}+2)^4}\right)-12 i \left(\frac{\text{lam0}}{112 (\text{lam0}+2)^3}-\frac{1}{112 (\text{lam0}+2)^2}+\frac{1}{56 (\text{lam0}+2)^3}\right)+\frac{i \text{lam0} \left(-\frac{1}{12 \sqrt{\text{lam0}+2}}+\frac{1}{14 (\text{lam0}+2)^{3/2}}-\frac{1}{28 (\text{lam0}+2)^{5/2}}\right)}{2 \sqrt{\text{lam0}+2}}-\frac{4 i \left(\frac{1}{16} (\text{lam0}+2)^{3/2}-\frac{5 \sqrt{\text{lam0}+2}}{48}+\frac{1}{16 \sqrt{\text{lam0}+2}}-\frac{1}{16 (\text{lam0}+2)^{3/2}}+\frac{1}{24 (\text{lam0}+2)^{5/2}}\right)}{3 \sqrt{\text{lam0}+2}}-\frac{i (-7 \text{lam0}-12) \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right)}{21 (\text{lam0}+2)^{3/2}}+\frac{i (-\text{lam0}-1)}{8 (\text{lam0}+2)^2}+\frac{5 i (3 \text{lam0}+5)}{72 (\text{lam0}+2)^3}+\frac{i (3 \text{lam0}+8) (33 \text{lam0}+56)}{16632 (\text{lam0}+2)^3}-\frac{i (3 \text{lam0}+5) \left(3 \text{lam0}^2+13 \text{lam0}+16\right)}{84 (\text{lam0}+2)^3}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i \text{dmu} (\text{lam0}+2) (\text{drho}+1)}{36 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i \text{dmu} (\text{drho}+1)}{54 (\text{lam0}+2)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-24 i (\text{lam0}+2)^{3/2} \left(-\frac{(-\text{lam0}-1) (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^4}{249480 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{(3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) \left(-\frac{1}{12 \sqrt{\text{lam0}+2}}+\frac{1}{14 (\text{lam0}+2)^{3/2}}-\frac{1}{28 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)^3}{630 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(-\frac{1}{72 \sqrt{\text{lam0}+2}}-\frac{1}{168 (\text{lam0}+2)^{3/2}}-\frac{1}{504 (\text{lam0}+2)^{5/2}}+\frac{1}{1512 (\text{lam0}+2)^{7/2}}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(26 \text{lam0}+45) (\text{drho}+1)}{1297296 (\text{lam0}+2)^{9/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (33 \text{lam0}+56) (\text{drho}+1)}{249480 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)}{630 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{4 (\text{dmu}+1) \left(\frac{1}{576 \sqrt{\text{lam0}+2}}+\frac{5}{1008 (\text{lam0}+2)^{3/2}}+\frac{1}{2016 (\text{lam0}+2)^{5/2}}+\frac{1}{33264 (\text{lam0}+2)^{7/2}}-\frac{1}{133056 (\text{lam0}+2)^{9/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{4 i \left(-\frac{i (\text{drho}+1)^4}{166320 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{10080 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{1008 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{1440 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{2880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{3 \sqrt{\text{lam0}+2}}-\frac{i (-7 \text{lam0}-12) \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{21 (\text{lam0}+2)^{3/2}}-\frac{i (3 \text{lam0}+5) \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{126 (\text{lam0}+2)^{5/2}}+144 i \left(\frac{i (\text{drho}+1)^4}{5239080 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^2}{5239080 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{77837760 (\text{lam0}+2)^{9/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{15567552 (\text{lam0}+2)^{9/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+12 i \left(-\frac{i (\text{drho}+1)^4}{249480 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{158760 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{158760 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{748440 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{187110 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)-2 i \sqrt{\text{lam0}+2} \left(\frac{(33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^4}{24948 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{(-\text{lam0}-1) (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (\text{drho}+1)^3}{630 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(-\frac{1}{12 \sqrt{\text{lam0}+2}}+\frac{1}{14 (\text{lam0}+2)^{3/2}}-\frac{1}{28 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (3 \text{lam0}+5) (\text{drho}+1)}{630 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(33 \text{lam0}+56) (\text{drho}+1)}{24948 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{4 (\text{dmu}+1) \left(-\frac{1}{72 \sqrt{\text{lam0}+2}}-\frac{1}{168 (\text{lam0}+2)^{3/2}}-\frac{1}{504 (\text{lam0}+2)^{5/2}}+\frac{1}{1512 (\text{lam0}+2)^{7/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{4 i \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{3 \sqrt{\text{lam0}+2}}-\frac{i (-7 \text{lam0}-12) \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{21 (\text{lam0}+2)^{3/2}}+144 i \left(-\frac{i (\text{drho}+1)^4}{249480 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{158760 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{158760 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{748440 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{187110 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+12 i \left(\frac{i (\text{drho}+1)^3}{5670 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)}{11340 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{3780 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)-\frac{2 i \left(\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^4}{83160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) \left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^3}{420 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(3 \text{lam0}+5) (\text{drho}+1)}{72 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{\sqrt{\text{lam0}+2} (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{4 (\text{dmu}+1) \left(\frac{1}{16} (\text{lam0}+2)^{3/2}-\frac{5 \sqrt{\text{lam0}+2}}{48}+\frac{1}{16 \sqrt{\text{lam0}+2}}-\frac{1}{16 (\text{lam0}+2)^{3/2}}+\frac{1}{24 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(\text{lam0}+2)^{3/2} (\text{drho}+1)}{24 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (\text{drho}+1)}{40 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-72 i (\text{lam0}+2)^{3/2} \left(-\frac{i (\text{drho}+1)^4}{166320 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{10080 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{1008 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{1440 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{2880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+\frac{i \text{lam0} \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{2 \sqrt{\text{lam0}+2}}-144 i \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{110880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{12096 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{10080 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{8640 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{3456 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-12 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)}{3 \sqrt{\text{lam0}+2}}+\frac{i \left(-\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (\text{drho}+1)^3}{210 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^2}{70 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{4 (\text{dmu}+1) \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{(\text{lam0}+2)^{3/2} (\text{drho}+1)}{2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{\text{drho}+1}{4 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) \text{lam0} (\text{drho}+1)}{10 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-72 i (\text{lam0}+2)^{3/2} \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-144 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-12 i \left(-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{84 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{60 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{120 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)}{7 (\text{lam0}+2)^{3/2}}-\frac{1}{2} i \left(\frac{(3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (5 \text{lam0}+10) (\text{drho}+1)^3}{630 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(3 \text{lam0}+5) (\text{drho}+1)}{126 (\text{lam0}+2) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(3 \text{lam0}-2) (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)}{315 (\text{lam0}+2) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(-7 \text{lam0}-12) (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)}{210 (\text{lam0}+2) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{4 (\text{dmu}+1) \left(-\frac{\text{lam0}}{8 (\text{lam0}+2)^2}+\frac{11}{24 (\text{lam0}+2)}-\frac{2}{6 \text{lam0}+12}-\frac{1}{4 (\text{lam0}+2)^2}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-72 i (\text{lam0}+2)^{3/2} \left(\frac{i (\text{drho}+1)^3}{5670 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)}{11340 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{3780 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-\frac{4 i \left(-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{84 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{60 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{120 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{3 \sqrt{\text{lam0}+2}}\right)-48 i \left(\frac{(3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) \left(-\frac{\text{lam0}}{8 (\text{lam0}+2)^2}+\frac{11}{24 (\text{lam0}+2)}-\frac{2}{6 \text{lam0}+12}-\frac{1}{4 (\text{lam0}+2)^2}\right) (\text{drho}+1)^3}{630 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(\frac{\text{lam0}}{112 (\text{lam0}+2)^3}-\frac{1}{112 (\text{lam0}+2)^2}+\frac{1}{56 (\text{lam0}+2)^3}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(26 \text{lam0}+45) (\text{drho}+1)}{2594592 (\text{lam0}+2)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{(33 \text{lam0}+56) (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)}{498960 (\text{lam0}+2)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)}{2520 (\text{lam0}+2)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{4 (\text{dmu}+1) \left(-\frac{\text{lam0}}{4032 (\text{lam0}+2)^4}+\frac{1}{4032 (\text{lam0}+2)^3}-\frac{1}{2016 (\text{lam0}+2)^4}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-72 i (\text{lam0}+2)^{3/2} \left(\frac{i (\text{drho}+1)^4}{5239080 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^2}{5239080 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{77837760 (\text{lam0}+2)^{9/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{15567552 (\text{lam0}+2)^{9/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(-\frac{i (\text{drho}+1)^4}{249480 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{158760 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{158760 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{748440 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{187110 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+\frac{i \text{lam0} \left(\frac{i (\text{drho}+1)^3}{5670 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)}{11340 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{3780 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{2 \sqrt{\text{lam0}+2}}-\frac{4 i \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{110880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{12096 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{10080 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{8640 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{3456 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{3 \sqrt{\text{lam0}+2}}-\frac{i (-7 \text{lam0}-12) \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{21 (\text{lam0}+2)^{3/2}}-\frac{i (3 \text{lam0}+5) \left(-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{84 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{60 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{120 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{126 (\text{lam0}+2)^{5/2}}\right)-3 i \left(-\frac{(5 \text{lam0}+10) (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^4}{249480 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(-\frac{\text{lam0}}{8 (\text{lam0}+2)^2}+\frac{11}{24 (\text{lam0}+2)}-\frac{2}{6 \text{lam0}+12}-\frac{1}{4 (\text{lam0}+2)^2}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(33 \text{lam0}+56) (\text{drho}+1)}{49896 (\text{lam0}+2)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(3 \text{lam0}+5) (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)}{1260 (\text{lam0}+2)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{\text{lam0} (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)}{630 (\text{lam0}+2)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{4 (\text{dmu}+1) \left(\frac{\text{lam0}}{112 (\text{lam0}+2)^3}-\frac{1}{112 (\text{lam0}+2)^2}+\frac{1}{56 (\text{lam0}+2)^3}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-72 i (\text{lam0}+2)^{3/2} \left(-\frac{i (\text{drho}+1)^4}{249480 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{158760 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{158760 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{748440 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{187110 (\text{lam0}+2)^{7/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(\frac{i (\text{drho}+1)^3}{5670 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)}{11340 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{3780 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-\frac{4 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{3 \sqrt{\text{lam0}+2}}-\frac{i (-7 \text{lam0}-12) \left(-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{84 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{60 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{120 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{21 (\text{lam0}+2)^{3/2}}\right)\right) (\text{dmu}+1)^2}{(\text{drho}+1)^3 \sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)}+\frac{25 i \sqrt{\text{lam0}+2} \left(-28 \text{dmu}^2-14 \text{dlam} \text{dmu}+18 \text{drho} \text{dmu}-14 \text{lam0} \text{dmu}-52 \text{dmu}+21 \text{dlam} \text{drho}+42 \text{drho}+21 \text{drho} \text{lam0}\right) \left(\frac{525 (\text{dmu}+1)^2 \left(-\frac{\sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) (\text{drho}+1)^5}{20790 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{\sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) (\text{drho}+1)^5}{29700 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{\left(42 \text{lam0}^2 \text{dmu}^2+182 \text{lam0} \text{dmu}^2+224 \text{dmu}^2-18 \text{drho} \text{lam0}^2 \text{dmu}+45 \text{lam0}^2 \text{dmu}-96 \text{drho} \text{dmu}-84 \text{drho} \text{lam0} \text{dmu}+217 \text{lam0} \text{dmu}+282 \text{dmu}-21 \text{drho} \text{lam0}^2-84 \text{drho}-84 \text{drho} \text{lam0}+35 \text{lam0}+70\right) (\text{drho}+1)^4}{8820 (\text{dmu}+1) \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{17 \left(-42 \text{dmu} \text{lam0}^3+21 \text{drho} \text{lam0}^3-84 \text{dmu}^2 \text{lam0}^2-42 \text{dlam} \text{dmu} \text{lam0}^2-272 \text{dmu} \text{lam0}^2+21 \text{dlam} \text{drho} \text{lam0}^2+36 \text{dmu} \text{drho} \text{lam0}^2+161 \text{drho} \text{lam0}^2-364 \text{dmu}^2 \text{lam0}-21 \text{dlam} \text{lam0}-182 \text{dlam} \text{dmu} \text{lam0}-658 \text{dmu} \text{lam0}+98 \text{dlam} \text{drho} \text{lam0}+168 \text{dmu} \text{drho} \text{lam0}+392 \text{drho} \text{lam0}-448 \text{dmu}^2-42 \text{dlam}-224 \text{dlam} \text{dmu}-564 \text{dmu}+112 \text{dlam} \text{drho}+192 \text{dmu} \text{drho}+308 \text{drho}\right) (\text{drho}+1)^4}{132300 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{lam0}+2)^{3/2} \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right) (\text{drho}+1)^3}{840 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{\left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right) (\text{drho}+1)^3}{1050 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{\sqrt{\text{lam0}+2} \left(-28 \text{dmu}^4-76 \text{dlam} \text{dmu}^3+220 \text{drho} \text{dmu}^3+110 \text{drho} \text{lam0} \text{dmu}^3+118 \text{lam0} \text{dmu}^3+52 \text{dmu}^3-31 \text{dlam}^2 \text{dmu}^2+161 \text{drho} \text{lam0}^2 \text{dmu}^2+196 \text{lam0}^2 \text{dmu}^2+50 \text{dlam} \text{dmu}^2+322 \text{dlam} \text{drho} \text{dmu}^2+1124 \text{drho} \text{dmu}^2+165 \text{dlam} \text{lam0} \text{dmu}^2+161 \text{dlam} \text{drho} \text{lam0} \text{dmu}^2+884 \text{drho} \text{lam0} \text{dmu}^2+864 \text{lam0} \text{dmu}^2+788 \text{dmu}^2+47 \text{drho} \text{lam0}^3 \text{dmu}+59 \text{lam0}^3 \text{dmu}+24 \text{dlam}^2 \text{dmu}+118 \text{dlam} \text{lam0}^2 \text{dmu}+94 \text{dlam} \text{drho} \text{lam0}^2 \text{dmu}+552 \text{drho} \text{lam0}^2 \text{dmu}+614 \text{lam0}^2 \text{dmu}+600 \text{dlam} \text{dmu}+94 \text{dlam}^2 \text{drho} \text{dmu}+916 \text{dlam} \text{drho} \text{dmu}+1588 \text{drho} \text{dmu}+59 \text{dlam}^2 \text{lam0} \text{dmu}+638 \text{dlam} \text{lam0} \text{dmu}+47 \text{dlam}^2 \text{drho} \text{lam0} \text{dmu}+646 \text{dlam} \text{drho} \text{lam0} \text{dmu}+1710 \text{drho} \text{lam0} \text{dmu}+1646 \text{lam0} \text{dmu}+1308 \text{dmu}+63 \text{drho} \text{lam0}^3+75 \text{lam0}^3+87 \text{dlam}^2+150 \text{dlam} \text{lam0}^2+126 \text{dlam} \text{drho} \text{lam0}^2+423 \text{drho} \text{lam0}^2+450 \text{lam0}^2+474 \text{dlam}+126 \text{dlam}^2 \text{drho}+594 \text{dlam} \text{drho}+684 \text{drho}+75 \text{dlam}^2 \text{lam0}+537 \text{dlam} \text{lam0}+63 \text{dlam}^2 \text{drho} \text{lam0}+549 \text{dlam} \text{drho} \text{lam0}+936 \text{drho} \text{lam0}+900 \text{lam0}+600\right) (\text{drho}+1)^3}{6300 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{dmu}+1) \left(-\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (\text{drho}+1)^3}{42 (\text{dmu}+1)^2}+\frac{9 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^2}{35 (\text{dmu}+1) \sqrt{\text{lam0}+2}}-\frac{3 (\text{lam0}+2)^{3/2} (\text{drho}+1)}{2 (\text{dmu}+1)}-\frac{4}{5} \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)-\frac{3 (\text{dmu}-\text{drho}) \text{lam0} (\text{drho}+1)}{10 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{3 (\text{drho}+1)}{4 (\text{dmu}+1) \sqrt{\text{lam0}+2}}-72 i \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) (\text{lam0}+2)^{3/2}+i \left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) \sqrt{\text{lam0}+2} (3 \text{lam0}-2)-12 i \left(-\frac{3 i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dmu}+1)^3}+\frac{11 i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{420 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{40 (\text{dmu}+1) \sqrt{\text{lam0}+2}}\right)-144 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right)\right) (\text{drho}+1)^2}{35 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{8 \left(-\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (\text{drho}+1)^3}{210 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^2}{70 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{4 (\text{dmu}+1) \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{(\text{lam0}+2)^{3/2} (\text{drho}+1)}{2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{\text{drho}+1}{4 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) \text{lam0} (\text{drho}+1)}{10 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-72 i (\text{lam0}+2)^{3/2} \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-144 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-12 i \left(-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{84 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{60 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{120 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right) (\text{drho}+1)^2}{105 (\text{dmu}+1)}+\frac{2 (\text{dmu}+1) \left(\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (\text{drho}+1)^4}{1980 (\text{dmu}+1)^3}-\frac{\left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^3}{84 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}+\frac{6 \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}+\frac{(\text{lam0}+2)^{3/2} (\text{drho}+1)}{8 (\text{dmu}+1)}+\frac{(3 \text{lam0}+5) (\text{drho}+1)}{24 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}+\frac{3 \sqrt{\text{lam0}+2} (-\text{dmu}+2 \text{drho}+\text{drho} \text{lam0}+\text{lam0}+1) (\text{drho}+1)}{20 (\text{dmu}+1)^2}-\frac{4}{5} \left(\frac{1}{16} (\text{lam0}+2)^{3/2}-\frac{5 \sqrt{\text{lam0}+2}}{48}+\frac{1}{16 \sqrt{\text{lam0}+2}}-\frac{1}{16 (\text{lam0}+2)^{3/2}}+\frac{1}{24 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)-\frac{3 (\text{dmu}-\text{drho}) (\text{drho}+1)}{40 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-72 i \left(-\frac{i (\text{drho}+1)^4}{18480 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{2016 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{960 (\text{dmu}+1)}\right) (\text{lam0}+2)^{3/2}+i \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) \sqrt{\text{lam0}+2} (3 \text{lam0}-2)-144 i \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{66528 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{60480 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}+\frac{i (\text{drho}+1)^2}{1120 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)}{1920 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}\right)-12 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right)+\frac{i \left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) \text{lam0}}{2 \sqrt{\text{lam0}+2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{2}{5} \left(\frac{\sqrt{\text{lam0}+2} (3 \text{lam0}+8) (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^4}{83160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) \left(3 \text{lam0}^2+13 \text{lam0}+16\right) (\text{drho}+1)^3}{420 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(-\frac{9}{8} (\text{lam0}+2)^{3/2}+\frac{\sqrt{\text{lam0}+2}}{2}+\frac{3}{8 \sqrt{\text{lam0}+2}}-\frac{3}{4 (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(3 \text{lam0}+5) (\text{drho}+1)}{72 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{\sqrt{\text{lam0}+2} (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{4 (\text{dmu}+1) \left(\frac{1}{16} (\text{lam0}+2)^{3/2}-\frac{5 \sqrt{\text{lam0}+2}}{48}+\frac{1}{16 \sqrt{\text{lam0}+2}}-\frac{1}{16 (\text{lam0}+2)^{3/2}}+\frac{1}{24 (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(\text{lam0}+2)^{3/2} (\text{drho}+1)}{24 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{(-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (\text{drho}+1)}{40 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-72 i (\text{lam0}+2)^{3/2} \left(-\frac{i (\text{drho}+1)^4}{166320 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{10080 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{1008 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{1440 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{2880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)+\frac{i \text{lam0} \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)}{2 \sqrt{\text{lam0}+2}}-144 i \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{110880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{12096 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{10080 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{8640 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{3456 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)-12 i \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right) (\text{drho}+1)-24 i (\text{lam0}+2)^{3/2} \left(-\frac{i (\text{dmu}-\text{drho}) (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^5}{415800 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (\text{drho}+1)^5}{29700 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (2 \text{dlam}+7 \text{dmu}+2 \text{lam0}+7) (\text{drho}+1)^3}{3150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{\left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (\text{drho}+1)^3}{630 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{\left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^3}{126 (\text{dmu}+1)^2}-\frac{\left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) (7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{6 \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}-\frac{4}{5} \left(-\frac{i (\text{drho}+1)^4}{166320 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{10080 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{1008 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{1440 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{2880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)+\frac{4 (\text{dmu}+1) \left(-\frac{i (\text{drho}+1)^4}{18480 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{2016 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{960 (\text{dmu}+1)}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+144 i \left(\frac{(\text{drho}+1)^6}{3492720 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(\text{drho}+1)^6}{1587600 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{17 (\text{drho}+1)^6}{52390800 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}\right)+12 i \left(-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{11 (\text{drho}+1)^5}{793800 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{drho}+1)^5}{66150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(-\text{drho}-1) (\text{drho}+1)^4}{1247400 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)-2 i \sqrt{\text{lam0}+2} \left(-\frac{i (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^5}{41580 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^5}{2970 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (\text{dmu}-\text{drho}) (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (\text{drho}+1)^4}{1050 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{2 i \left(2 \text{dlam}^2+17 \text{dmu} \text{dlam}+4 \text{lam0} \text{dlam}+17 \text{dlam}+29 \text{dmu}^2+2 \text{lam0}^2+58 \text{dmu}+17 \text{dmu} \text{lam0}+17 \text{lam0}+29\right) (\text{drho}+1)^4}{4725 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (-\text{dlam}-2 \text{dmu}+\text{drho}-\text{lam0}-1) (\text{drho}+1)^4}{630 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{\left(\frac{3 i (\text{drho}+1)^3}{140 (\text{dmu}+1)^3}-\frac{i (\text{drho}+1)^2}{14 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{20 (\text{dmu}+1)}\right) (7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{6 \left(\frac{i (\text{drho}+1)^3}{140 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{70 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{30 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{20 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}-\frac{4}{5} \left(-\frac{i (\text{drho}+1)^4}{7560 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i (\text{drho}+1)^3}{2520 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{840 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{90 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)+\frac{4 (\text{dmu}+1) \left(-\frac{i (\text{drho}+1)^4}{2520 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{360 (\text{dmu}+1)^3}-\frac{3 i (\text{drho}+1)^2}{280 (\text{dmu}+1)^2}+\frac{i (\text{drho}+1)}{120 (\text{dmu}+1)}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+144 i \left(-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{11 (\text{drho}+1)^5}{793800 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{drho}+1)^5}{66150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(-\text{drho}-1) (\text{drho}+1)^4}{1247400 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)-3 i \left(\frac{i (\text{lam0}+2)^{3/2} (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (\text{drho}+1)^5}{83160 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^5}{5940 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i \sqrt{\text{lam0}+2} (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)^4}{1260 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i \sqrt{\text{lam0}+2} (3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) (-\text{dmu}+2 \text{drho}+\text{drho} \text{lam0}+\text{lam0}+1) (\text{drho}+1)^4}{2100 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(2 \text{dlam}^2+17 \text{dmu} \text{dlam}+4 \text{lam0} \text{dlam}+17 \text{dlam}+29 \text{dmu}^2+2 \text{lam0}^2+58 \text{dmu}+17 \text{dmu} \text{lam0}+17 \text{lam0}+29\right) (\text{drho}+1)^4}{28350 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i \text{lam0} (2 \text{dlam}+7 \text{dmu}+2 \text{lam0}+7) (\text{drho}+1)^3}{3150 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(-\frac{3 i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dmu}+1)^3}+\frac{11 i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{420 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{40 (\text{dmu}+1) \sqrt{\text{lam0}+2}}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{6 \left(-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{84 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{60 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{120 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}+\frac{4 (\text{dmu}+1) \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{4}{5} \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)-72 i (\text{lam0}+2)^{3/2} \left(-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{11 (\text{drho}+1)^5}{793800 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{drho}+1)^5}{66150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(-\text{drho}-1) (\text{drho}+1)^4}{1247400 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)-48 i \left(\frac{i \sqrt{\text{lam0}+2} (-\text{dlam}-2 \text{dmu}+2 \text{drho}+\text{drho} \text{lam0}) (\text{drho}+1)^5}{59400 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i \sqrt{\text{lam0}+2} (33 \text{dlam}+56 \text{dmu}+33 \text{lam0}+56) (-\text{dmu}+2 \text{drho}+\text{drho} \text{lam0}+\text{lam0}+1) (\text{drho}+1)^5}{831600 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}+\frac{i \text{lam0} \left(2 \text{dlam}^2+17 \text{dmu} \text{dlam}+4 \text{lam0} \text{dlam}+17 \text{dlam}+29 \text{dmu}^2+2 \text{lam0}^2+58 \text{dmu}+17 \text{dmu} \text{lam0}+17 \text{lam0}+29\right) (\text{drho}+1)^4}{56700 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (2 \text{dlam}+7 \text{dmu}+2 \text{lam0}+7) (\text{drho}+1)^3}{12600 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(3 \text{dlam}+5 \text{dmu}+3 \text{lam0}+5) \left(-\frac{3 i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dmu}+1)^3}+\frac{11 i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{420 (\text{dmu}+1)^2}-\frac{i (\text{drho}+1)}{40 (\text{dmu}+1) \sqrt{\text{lam0}+2}}\right) (\text{drho}+1)^3}{630 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{\left(-\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^3}{280 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^2}{84 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{60 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{120 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^3}{126 (\text{dmu}+1)^2}-\frac{(7 \text{dlam}+12 \text{dmu}+7 \text{lam0}+12) \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{5040 (\text{dmu}+1)^4}-\frac{13 i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{15120 (\text{dmu}+1)^3}+\frac{i (\text{drho}+1)^2}{1680 (\text{dmu}+1)^2 \sqrt{\text{lam0}+2}}-\frac{i (\text{drho}+1)}{240 (\text{dmu}+1) (\text{lam0}+2)^{3/2}}\right) (\text{drho}+1)^2}{105 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{6 \left(\frac{i (\text{lam0}+2)^{3/2} (\text{drho}+1)^4}{15120 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^3}{2160 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{i (\text{drho}+1)^2}{560 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{i (\text{drho}+1)}{360 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}+\frac{i (-\text{drho}-1)}{240 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)^2}{35 (\text{dmu}+1)}+\frac{4 (\text{dmu}+1) \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{66528 (\text{dmu}+1)^4}+\frac{i (\text{drho}+1)^3}{60480 (\text{dmu}+1)^3 \sqrt{\text{lam0}+2}}+\frac{i (\text{drho}+1)^2}{1120 (\text{dmu}+1)^2 (\text{lam0}+2)^{3/2}}+\frac{i (\text{drho}+1)}{1920 (\text{dmu}+1) (\text{lam0}+2)^{5/2}}\right) (\text{drho}+1)}{15 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{4}{5} \left(\frac{i \sqrt{\text{lam0}+2} (\text{drho}+1)^4}{110880 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{i (\text{drho}+1)^3}{12096 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{i (\text{drho}+1)^2}{10080 (\text{lam0}+2)^{3/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{i (\text{drho}+1)}{8640 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{i (-\text{drho}-1)}{3456 (\text{lam0}+2)^{5/2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right) (\text{drho}+1)-72 i (\text{lam0}+2)^{3/2} \left(\frac{(\text{drho}+1)^6}{3492720 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}+\frac{(\text{drho}+1)^6}{1587600 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}+\frac{17 (\text{drho}+1)^6}{52390800 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}\right)+i \sqrt{\text{lam0}+2} (3 \text{lam0}-2) \left(-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}-\frac{11 (\text{drho}+1)^5}{793800 (\text{dmu}+1)^3 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}-\frac{(\text{drho}+1)^5}{66150 (\text{dmu}+1)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^3}-\frac{(\text{drho}+1)^5}{415800 (\text{dmu}+1) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^4}-\frac{(-\text{drho}-1) (\text{drho}+1)^4}{1247400 (\text{dmu}+1)^4 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)}\right)\right)\right) (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}{(\text{drho}+1)^3 \sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)}+\frac{\left(504 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^5+4 \left(702 \text{lam0}^3+6036 \text{lam0}^2+16771 \text{lam0}-73 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+16094\right) \text{dmu}^4+2 \left(513 \text{lam0}^4+8976 \text{lam0}^3+45815 \text{lam0}^2+96812 \text{lam0}-\text{drho} \left(699 \text{lam0}^3+6745 \text{lam0}^2+19504 \text{lam0}+17620\right)+75620\right) \text{dmu}^3-(\text{lam0}+2) \left(-3147 \text{lam0}^3-26458 \text{lam0}^2-72604 \text{lam0}+\text{drho} \left(444 \text{lam0}^3+8399 \text{lam0}^2+35576 \text{lam0}+42092\right)-70804\right) \text{dmu}^2-3 (\text{lam0}+2)^2 \left(\text{drho} \left(505 \text{lam0}^2+4024 \text{lam0}+6778\right)-5 \left(151 \text{lam0}^2+604 \text{lam0}+778\right)\right) \text{dmu}-45 \text{drho} (\text{lam0}+2)^3 (27 \text{lam0}+77)+\text{dlam}^2 \left(342 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^3+\left(3147 \text{lam0}^2+13885 \text{lam0}-148 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+17126\right) \text{dmu}^2-3 \left(-755 \text{lam0}^2-3632 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+2224 \text{lam0}+2428\right)-4664\right) \text{dmu}-9 (\text{lam0}+2) (\text{drho} (135 \text{lam0}+298)-105)\right)+\text{dlam} \left(936 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^4+\left(2052 \text{lam0}^3+22398 \text{lam0}^2+70048 \text{lam0}-466 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+72968\right) \text{dmu}^3+\left(6294 \text{lam0}^3+46637 \text{lam0}^2+120308 \text{lam0}-\text{drho} \left(888 \text{lam0}^3+11359 \text{lam0}^2+37012 \text{lam0}+35692\right)+108596\right) \text{dmu}^2-6 (\text{lam0}+2) \left(-755 \text{lam0}^2-3326 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+3124 \text{lam0}+4402\right)-4532\right) \text{dmu}-9 (\text{lam0}+2)^2 (\text{drho} (270 \text{lam0}+683)-105)\right)\right)^2}{324 (\text{dmu}+1)^2 (\text{lam0}+2)^2 (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30)^2 \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)^2 (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2}\right) (\text{dmu}+1)^2}{(\text{drho}+1)^2 (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30) \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)}+\frac{25 i \left(168 \text{dmu}^3+12 (14 \text{dlam}-6 \text{drho}+14 \text{lam0}+29) \text{dmu}^2+2 \left(21 \text{dlam}^2+(-39 \text{drho}+42 \text{lam0}+87) \text{dlam}+5 \text{drho}^2+21 \text{lam0}^2-77 \text{drho}-39 \text{drho} \text{lam0}+87 \text{lam0}+86\right) \text{dmu}-3 \text{drho} (\text{dlam}+\text{lam0}+2) (7 \text{dlam}-3 \text{drho}+7 \text{lam0}+11)\right) \left(504 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^5+4 \left(702 \text{lam0}^3+6036 \text{lam0}^2+16771 \text{lam0}-73 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+16094\right) \text{dmu}^4+2 \left(513 \text{lam0}^4+8976 \text{lam0}^3+45815 \text{lam0}^2+96812 \text{lam0}-\text{drho} \left(699 \text{lam0}^3+6745 \text{lam0}^2+19504 \text{lam0}+17620\right)+75620\right) \text{dmu}^3-(\text{lam0}+2) \left(-3147 \text{lam0}^3-26458 \text{lam0}^2-72604 \text{lam0}+\text{drho} \left(444 \text{lam0}^3+8399 \text{lam0}^2+35576 \text{lam0}+42092\right)-70804\right) \text{dmu}^2-3 (\text{lam0}+2)^2 \left(\text{drho} \left(505 \text{lam0}^2+4024 \text{lam0}+6778\right)-5 \left(151 \text{lam0}^2+604 \text{lam0}+778\right)\right) \text{dmu}-45 \text{drho} (\text{lam0}+2)^3 (27 \text{lam0}+77)+\text{dlam}^2 \left(342 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^3+\left(3147 \text{lam0}^2+13885 \text{lam0}-148 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+17126\right) \text{dmu}^2-3 \left(-755 \text{lam0}^2-3632 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+2224 \text{lam0}+2428\right)-4664\right) \text{dmu}-9 (\text{lam0}+2) (\text{drho} (135 \text{lam0}+298)-105)\right)+\text{dlam} \left(936 \left(3 \text{lam0}^2+13 \text{lam0}+16\right) \text{dmu}^4+\left(2052 \text{lam0}^3+22398 \text{lam0}^2+70048 \text{lam0}-466 \text{drho} \left(3 \text{lam0}^2+14 \text{lam0}+16\right)+72968\right) \text{dmu}^3+\left(6294 \text{lam0}^3+46637 \text{lam0}^2+120308 \text{lam0}-\text{drho} \left(888 \text{lam0}^3+11359 \text{lam0}^2+37012 \text{lam0}+35692\right)+108596\right) \text{dmu}^2-6 (\text{lam0}+2) \left(-755 \text{lam0}^2-3326 \text{lam0}+\text{drho} \left(505 \text{lam0}^2+3124 \text{lam0}+4402\right)-4532\right) \text{dmu}-9 (\text{lam0}+2)^2 (\text{drho} (270 \text{lam0}+683)-105)\right)\right) (\text{dmu}+1)}{108 (\text{drho}+1)^2 \sqrt{\text{lam0}+2} (\text{dlam}+2 \text{dmu}+\text{lam0}+2)^2 (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30)^2 \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)^2}\right) \text{rvar}^2}{15 (\text{dmu}+1)}\right)}{\text{rvar}}\right) w^5+\frac{20 \text{dmu} \left(3 \sqrt{\text{lam0}+2} \text{lam0}^2+12 \sqrt{\text{lam0}+2} \text{lam0}+12 \sqrt{\text{lam0}+2}+2\right) \text{rvar} w^4}{3 (\text{lam0}+2) (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30)^2}+\frac{5 i \text{rvar} \left(-84 \text{lam0}^2 \text{dmu}^3-364 \text{lam0} \text{dmu}^3-448 \text{dmu}^3-108 \text{lam0}^3 \text{dmu}^2-108 \text{dlam} \text{lam0}^2 \text{dmu}^2+36 \text{drho} \text{lam0}^2 \text{dmu}^2-934 \text{lam0}^2 \text{dmu}^2+12 \text{lam0}^3 \text{rvar}^2 \text{dmu}^2+12 \text{dlam} \text{lam0}^2 \text{rvar}^2 \text{dmu}^2-12 \text{drho} \text{lam0}^2 \text{rvar}^2 \text{dmu}^2+44 \text{lam0}^2 \text{rvar}^2 \text{dmu}^2+64 \text{dlam} \text{rvar}^2 \text{dmu}^2-64 \text{drho} \text{rvar}^2 \text{dmu}^2+56 \text{dlam} \text{lam0} \text{rvar}^2 \text{dmu}^2-56 \text{drho} \text{lam0} \text{rvar}^2 \text{dmu}^2+8 \text{lam0} \text{rvar}^2 \text{dmu}^2-64 \text{rvar}^2 \text{dmu}^2-576 \text{dlam} \text{dmu}^2+192 \text{drho} \text{dmu}^2-466 \text{dlam} \text{lam0} \text{dmu}^2+168 \text{drho} \text{lam0} \text{dmu}^2-2606 \text{lam0} \text{dmu}^2-2524 \text{dmu}^2+48 \text{drho} \text{lam0}^3 \text{dmu}-210 \text{lam0}^3 \text{dmu}-210 \text{dlam} \text{lam0}^2 \text{dmu}+48 \text{dlam} \text{drho} \text{lam0}^2 \text{dmu}+440 \text{drho} \text{lam0}^2 \text{dmu}-1290 \text{lam0}^2 \text{dmu}-18 \text{drho} \text{lam0}^3 \text{rvar}^2 \text{dmu}+30 \text{lam0}^3 \text{rvar}^2 \text{dmu}+30 \text{dlam} \text{lam0}^2 \text{rvar}^2 \text{dmu}-18 \text{dlam} \text{drho} \text{lam0}^2 \text{rvar}^2 \text{dmu}-174 \text{drho} \text{lam0}^2 \text{rvar}^2 \text{dmu}+30 \text{lam0}^2 \text{rvar}^2 \text{dmu}+120 \text{dlam} \text{rvar}^2 \text{dmu}-96 \text{dlam} \text{drho} \text{rvar}^2 \text{dmu}-440 \text{drho} \text{rvar}^2 \text{dmu}+120 \text{dlam} \text{lam0} \text{rvar}^2 \text{dmu}-84 \text{dlam} \text{drho} \text{lam0} \text{rvar}^2 \text{dmu}-496 \text{drho} \text{lam0} \text{rvar}^2 \text{dmu}-280 \text{lam0} \text{rvar}^2 \text{dmu}-440 \text{rvar}^2 \text{dmu}-1178 \text{dlam} \text{dmu}+256 \text{dlam} \text{drho} \text{dmu}+1088 \text{drho} \text{dmu}-939 \text{dlam} \text{lam0} \text{dmu}+224 \text{dlam} \text{drho} \text{lam0} \text{dmu}+1232 \text{drho} \text{lam0} \text{dmu}-2890 \text{lam0} \text{dmu}-2300 \text{dmu}+105 \text{drho} \text{lam0}^3+105 \text{dlam} \text{drho} \text{lam0}^2+700 \text{drho} \text{lam0}^2-45 \text{drho} \text{lam0}^3 \text{rvar}^2-45 \text{dlam} \text{drho} \text{lam0}^2 \text{rvar}^2-330 \text{drho} \text{lam0}^2 \text{rvar}^2-150 \text{lam0}^2 \text{rvar}^2-180 \text{dlam} \text{drho} \text{rvar}^2-600 \text{drho} \text{rvar}^2-180 \text{dlam} \text{drho} \text{lam0} \text{rvar}^2-780 \text{drho} \text{lam0} \text{rvar}^2-600 \text{lam0} \text{rvar}^2-600 \text{rvar}^2-210 \text{dlam}+476 \text{dlam} \text{drho}+1120 \text{drho}-105 \text{dlam} \text{lam0}+448 \text{dlam} \text{drho} \text{lam0}+1540 \text{drho} \text{lam0}\right) w^3}{\sqrt{\text{lam0}+2} (6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30)^2 \left(14 \text{dmu}^2+19 \text{dlam} \text{dmu}+19 \text{lam0} \text{dmu}+84 \text{dmu}+35 \text{dlam}+35 \text{lam0}+70\right)}+\frac{10 i \sqrt{\text{lam0}+2} \text{rvar} w}{6 \text{lam0} \text{dmu}+16 \text{dmu}+15 \text{lam0}+30}
\label{eq:mie-int-ur-2}
\end{equation}

